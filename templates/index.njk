---
layout: layout.njk
title: CipherLab - Cryptography Educational Platform
---
<section class="hero" id="home">
    <div class="hero-gradient-container"></div>
    <div class="row hero-row">
        <div class="col-12 text-center">
            <div class="typing-container mb-4">
                <div class="welcome-container">
                    <svg width="100%" height="100" viewBox="0 0 700 100" class="welcome-svg" preserveAspectRatio="xMidYMid meet">
                        <text x="50%" y="60" class="welcome-text-base" text-anchor="middle">Welcome to CipherLab</text>
                        <text x="50%" y="60" class="welcome-text-glitch welcome-text-glitch1" text-anchor="middle">Welcome to CipherLab</text>
                        <text x="50%" y="60" class="welcome-text-glitch welcome-text-glitch2" text-anchor="middle">Welcome to CipherLab</text>
                    </svg>
                </div>
            </div>
            <div class="hero-content">
                <p class="mb-4 fade-in hero-subtitle">Unlock the secrets of cryptography through interactive learning and challenges.</p>
                <div class="cta-buttons fade-in">
                    <a href="/learn/" class="cta-button"><i class="fas fa-rocket"></i> Get Started</a>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Animated binary code decoration -->
    <div class="binary-stream">01001100 01000101 01000001 01010010 01001110</div>
</section>

<section class="container section">
    <!-- Wrap heading in its own row and col for proper centering -->
    <div class="row">
        <div class="col-12 text-center">
            <h2 class="section-title mb-4 fade-in centered-heading visible">Explore Cryptography</h2>
        </div>
    </div>
    
    <div class="features-container">
        <div class="features-grid">
            <div class="feature-box fade-in visible">
                <div class="feature-icon-container">
                    <i class="fas fa-key feature-icon"></i>
                </div>
                <h2>Classic Ciphers</h2>
                <p>Learn about fundamental encryption techniques such as the Caesar cipher, Vigenère cipher, and substitution ciphers that laid the groundwork for modern cryptography.</p>
                <div class="feature-button">
                    <a href="/learn/#module1" class="btn-learn-more">Learn More <i class="fas fa-arrow-right"></i></a>
                </div>
            </div>
            
            <div class="feature-box fade-in visible">
                <div class="feature-icon-container">
                    <i class="fas fa-shield-alt feature-icon"></i>
                </div>
                <h2>Modern Encryption</h2>
                <p>Explore contemporary encryption algorithms like AES, RSA, and SHA that power today's secure communications, digital signatures, and blockchain technology.</p>
                <div class="feature-button">
                    <a href="/learn/#module2" class="btn-learn-more">Discover <i class="fas fa-arrow-right"></i></a>
                </div>
            </div>
            
            <div class="feature-box fade-in visible">
                <div class="feature-icon-container">
                    <i class="fas fa-unlock feature-icon"></i>
                </div>
                <h2>Cryptanalysis</h2>
                <p>Try hands-on challenges to analyze and break different ciphers, enhancing your problem-solving skills and understanding of security vulnerabilities.</p>
                <div class="feature-button">
                    <a href="/learn/#module5" class="btn-learn-more">Challenge Yourself <i class="fas fa-arrow-right"></i></a>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Additional CSS for this page -->
<style>
    /* --- CORE FIXES --- */
    /* Fix horizontal overflow and optimize render performance */
    html, body {
        max-width: 100%;
        overflow-x: hidden;
        box-sizing: border-box;
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
        /* Improve scrolling performance */
        scroll-behavior: auto !important;
        overscroll-behavior: none;
    }
    
    body * {
        box-sizing: border-box;
    }
    
    /* --- GRADIENT FIX (Separate element) --- */
    .hero-gradient-container {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        width: 100%;
        height: 100%;
        overflow: visible;
        z-index: -1;
        pointer-events: none;
        /* Fix scrolling issue by removing expensive filters */
        will-change: transform;
    }
    
    .hero-gradient-container::after {
        content: "";
        position: absolute;
        top: -100%; /* Extended far beyond container */
        left: -100%; /* Extended far beyond container */
        width: 300%; /* Much wider than needed */
        height: 300%; /* Much taller than needed */
        background: radial-gradient(
            circle at center,
            rgba(0, 255, 65, 0.15) 0%, 
            rgba(0, 255, 65, 0.08) 30%,
            rgba(0, 255, 65, 0.05) 40%,
            rgba(0, 255, 65, 0.02) 60%,
            rgba(0, 255, 65, 0.01) 70%,
            transparent 80%
        );
        z-index: -1;
        /* GPU acceleration */
        transform: translateZ(0);
        -webkit-transform: translateZ(0);
        backface-visibility: hidden;
        -webkit-backface-visibility: hidden;
    }
    
    /* Force center align */
    .text-center {
        text-align: center !important;
    }
    
    /* --- HERO SECTION --- */
    .hero {
        position: relative;
        min-height: 85vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
        padding: 3rem 0;
        overflow: visible; /* Critical for gradient visibility */
        width: 100%;
        max-width: 100%;
        /* Fix scrolling issue */
        contain: layout style paint;
    }
    
    /* Force row to be full-width */
    .hero-row {
        width: 100%;
        max-width: 100%;
        margin: 0 !important;
        padding: 0 1rem;
        overflow: visible;
    }
    
    /* Force column to be block displayed */
    .hero .col-12 {
        display: block !important;
        float: none !important;
        clear: both !important;
        margin-left: auto !important;
        margin-right: auto !important;
        max-width: 100%;
        overflow: visible;
    }
    
    /* --- WELCOME TEXT --- */
    .typing-container {
        width: 100% !important;
        position: relative !important;
        margin-bottom: 3rem !important;
        text-align: center !important;
        display: block !important;
        overflow: visible;
    }
    
    .welcome-container {
        display: block;
        text-align: center;
        margin: 0 auto;
        position: relative;
        width: 100%;
        max-width: 90rem;
        padding: 0 1rem;
        overflow: visible;
    }
    
    /* SVG container for welcome text */
    .welcome-svg {
        max-width: 100%;
        height: auto;
        overflow: visible;
        /* Fix rendering issues */
        contain: strict;
    }
    
    .welcome-text-base {
        font-family: 'IBM Plex Mono', monospace;
        font-weight: 700;
        font-size: 3rem; /* Desktop size */
        fill: var(--primary);
        filter: drop-shadow(0 0 2px var(--primary)) drop-shadow(0 0 5px var(--primary));
        transition: all 0.3s ease;
        animation: pulse-glow 3s infinite alternate;
        letter-spacing: -0.5px;
    }
    
    .welcome-text-glitch {
        font-family: 'IBM Plex Mono', monospace;
        font-weight: 700;
        font-size: 3rem; /* Desktop size */
        opacity: 0.65;
        position: absolute;
        top: 0;
        left: 0;
        letter-spacing: -0.5px;
    }
    
    .welcome-text-glitch1 {
        fill: var(--primary);
        filter: drop-shadow(0 0 2px var(--primary));
        animation: hard-glitch1 2s infinite steps(1);
        clip-path: polygon(0 0, 100% 0, 100% 45%, 0 45%);
    }
    
    .welcome-text-glitch2 {
        fill: var(--primary); 
        filter: drop-shadow(0 0 2px var(--primary));
        animation: hard-glitch2 1.5s infinite steps(1);
        clip-path: polygon(0 60%, 100% 60%, 100% 100%, 0 100%);
    }
    
    /* --- HERO CONTENT --- */
    .hero-content {
        margin-top: 2rem !important;
        position: relative !important;
        display: block !important;
        width: 100% !important;
        padding: 0 1rem;
    }
    
    /* Hero subtitle styling */
    .hero-subtitle {
        font-size: 1.5rem !important;
        line-height: 1.6 !important;
        color: var(--text) !important;
        transition: all 0.3s ease !important;
        transform: translateZ(0) !important;
        margin-top: 1rem !important;
        margin-bottom: 1.5rem !important;
        max-width: 800px !important;
        margin-left: auto !important;
        margin-right: auto !important;
    }
    
    /* CTA buttons styling */
    .cta-buttons {
        margin-top: 1rem !important;
        position: relative !important;
        z-index: 2 !important;
        display: block !important;
    }
    
    .cta-button {
        display: inline-block;
        padding: 1rem 2.5rem;
        font-size: 1.2rem;
        border-radius: 4px;
        transition: all 0.3s ease;
        background: linear-gradient(45deg, var(--primary-dark), var(--primary));
        color: var(--dark);
        border: 1px solid var(--primary);
        text-decoration: none;
        font-weight: 700;
        transform: translateZ(0);
    }
    
    /* Binary stream animation */
    .binary-stream {
        position: absolute;
        bottom: 20px;
        font-family: 'IBM Plex Mono', monospace;
        color: var(--primary);
        opacity: 0.5;
        font-size: 1rem;
        white-space: nowrap;
        animation: moveLeft 15s linear infinite;
        overflow: hidden;
        width: 100%;
        padding-right: 100vw;
        /* Optimize animation */
        will-change: transform;
        transform: translateZ(0);
    }
    
    @keyframes moveLeft {
        0% { transform: translateX(100vw); }
        100% { transform: translateX(-100%); }
    }
    
    /* --- FEATURES SECTION --- */
    .section-title.centered-heading {
        text-align: center !important;
        width: 100% !important;
        max-width: 100% !important;
        margin-left: auto !important;
        margin-right: auto !important;
        display: block !important;
    }
    
    .features-container {
        padding: 3rem 0;
        width: 100%;
        max-width: 1200px;
        margin: 0 auto;
        overflow: visible;
    }
    
    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
        width: 100%;
        /* Fix layout shifts */
        contain: layout style;
    }
    
    .feature-box {
        background-color: var(--medium-dark);
        border: 1px solid var(--primary);
        border-radius: 12px;
        padding: 2.5rem;
        position: relative;
        transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        overflow: visible;
        box-shadow: none !important;
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        height: 100%;
        border-right: 1px solid var(--primary) !important;
        border-right-width: 1px !important;
        border-right-style: solid !important;
        border-right-color: var(--primary) !important;
        max-width: 100%;
        /* Optimize animations */
        will-change: transform, box-shadow;
    }
    
    .feature-box::before, .feature-box::after {
        content: none !important;
        display: none !important;
    }
    
    .feature-box:hover {
        transform: translateY(-15px);
        box-shadow: 0 15px 30px rgba(0, 0, 0, 0.4) !important;
        border-color: var(--secondary);
        border-right-color: var(--secondary) !important;
    }
    
    .feature-icon-container {
        width: 5rem;
        height: 5rem;
        border-radius: 50%;
        background: linear-gradient(135deg, var(--primary-dark), var(--primary));
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 1.5rem;
        position: relative;
        box-shadow: 0 0 15px var(--primary-dark);
        z-index: 1;
        transition: all 0.3s ease;
    }
    
    .feature-icon {
        font-size: 2.5rem;
        color: var(--dark);
        transition: all 0.3s ease;
        line-height: 1;
        margin: 0;
        padding: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        height: 100%;
    }
    
    .feature-box:hover .feature-icon-container {
        transform: scale(1.1) rotate(5deg);
        background: linear-gradient(135deg, var(--secondary), var(--primary));
        box-shadow: 0 0 20px var(--secondary);
    }
    
    .feature-box h2 {
        color: var(--primary);
        font-size: 1.8rem;
        margin-bottom: 1.2rem;
        transition: all 0.3s ease;
        position: relative;
        z-index: 1;
    }
    
    .feature-box h2::after {
        content: "";
        position: absolute;
        bottom: -8px;
        left: 50%;
        transform: translateX(-50%);
        width: 40px;
        height: 3px;
        background-color: var(--primary);
        transition: all 0.3s ease;
    }
    
    .feature-box:hover h2 {
        color: var(--secondary);
        text-shadow: 0 0 5px var(--secondary);
    }
    
    .feature-box:hover h2::after {
        width: 60px;
        background-color: var(--secondary);
    }
    
    .feature-box p {
        color: var(--text);
        font-size: 1rem;
        line-height: 1.7;
        margin-bottom: 1.5rem;
        z-index: 1;
        position: relative;
        flex-grow: 1;
    }
    
    .feature-button {
        margin-top: auto;
        padding-top: 1.5rem;
    }
    
    .btn-learn-more {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        color: var(--primary);
        text-decoration: none;
        font-weight: 600;
        padding: 0.5rem 0;
        transition: all 0.3s ease;
        position: relative;
        z-index: 1;
    }
    
    .btn-learn-more::after {
        content: "";
        position: absolute;
        bottom: 0;
        left: 0;
        width: 0;
        height: 2px;
        background-color: var(--secondary);
        transition: width 0.3s ease;
    }
    
    .btn-learn-more:hover {
        color: var(--secondary);
        transform: translateX(3px);
    }
    
    .btn-learn-more:hover::after {
        width: 100%;
    }
    
    .section-title {
        font-size: 2.5rem;
        font-weight: 700;
        color: var(--primary);
        position: relative;
        display: inline-block;
        margin-bottom: 3rem;
        text-shadow: 0 0 5px var(--primary);
    }
    
    .section-title::after {
        content: "";
        position: absolute;
        bottom: -15px;
        left: 50%;
        transform: translateX(-50%);
        width: 80px;
        height: 3px;
        background: linear-gradient(to right, var(--primary), var(--secondary));
        transition: width 0.3s ease;
    }
    
    /* Fix for scrolling issues */
    .fade-in {
        will-change: opacity, transform;
        backface-visibility: hidden;
        -webkit-backface-visibility: hidden;
        transform: translateZ(0);
        -webkit-transform: translateZ(0);
    }
    
    /* --- RESPONSIVE SCALING --- */
    @media (max-width: 1400px) {
        .welcome-text-base,
        .welcome-text-glitch {
            font-size: 2.8rem;
        }
        
        .hero-subtitle {
            font-size: 1.4rem !important;
        }
    }
    
    @media (max-width: 1200px) {
        .welcome-text-base,
        .welcome-text-glitch {
            font-size: 2.6rem;
        }
        
        .hero-subtitle {
            font-size: 1.3rem !important;
        }
    }
    
    @media (max-width: 992px) {
        .welcome-text-base,
        .welcome-text-glitch {
            font-size: 2.4rem;
        }
        
        .welcome-svg {
            height: auto;
            min-height: 80px;
        }
        
        .hero-subtitle {
            font-size: 1.25rem !important;
        }
        
        .features-grid {
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 1.5rem;
        }
        
        .section-title {
            font-size: 2.2rem;
        }
        
        .hero {
            min-height: 80vh;
        }
    }
    
    @media (max-width: 768px) {
        .welcome-text-base,
        .welcome-text-glitch {
            font-size: 2.5rem; /* Increased from 2.2rem */
        }
        
        .welcome-svg {
            height: auto;
            min-height: 70px;
        }
        
        .feature-box {
            padding: 2rem;
            margin-bottom: 1.5rem;
            min-height: auto;
        }
        
        .hero {
            min-height: 70vh;
            padding: 2rem 1rem;
        }
        
        .hero-subtitle {
            font-size: 1.2rem !important;
            padding: 0 1rem;
        }
        
        .typing-container {
            margin-bottom: 2rem !important;
        }
        
        .feature-icon-container {
            width: 4.5rem;
            height: 4.5rem;
        }
        
        .feature-icon {
            font-size: 2.2rem;
        }
        
        .section-title {
            font-size: 2rem;
            margin-bottom: 2.5rem;
        }
        
        .cta-button {
            padding: 1rem 2rem;
            min-width: 200px;
        }
        
        .btn-learn-more {
            padding: 0.7rem 0;
            margin-top: 0.5rem;
            min-height: 44px;
            display: inline-flex;
            align-items: center;
        }
        
        .features-grid {
            gap: 2rem;
        }
    }
    
    @media (max-width: 576px) {
        .welcome-text-base,
        .welcome-text-glitch {
            font-size: 2.6rem; /* Increased from 2rem for mobile */
        }
        
        .welcome-svg {
            height: auto;
            min-height: 70px; /* Increased from 60px */
        }
        
        .welcome-svg text {
            dominant-baseline: middle;
        }
        
        .features-grid {
            grid-template-columns: 1fr; /* Force single column on phones */
        }
        
        .feature-box {
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            width: 100%;
        }
        
        .hero {
            min-height: 60vh;
            padding: 1.5rem 1rem;
        }
        
        .hero-subtitle {
            font-size: 1.1rem !important;
            padding: 0 0.5rem;
        }
        
        .typing-container {
            margin-bottom: 1.5rem !important;
        }
        
        .feature-icon-container {
            width: 4rem;
            height: 4rem;
            margin-bottom: 1rem;
        }
        
        .feature-icon {
            font-size: 1.8rem;
        }
        
        .feature-box h2 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }
        
        .feature-box p {
            font-size: 0.95rem;
            line-height: 1.6;
        }
        
        .section-title {
            font-size: 1.7rem;
            margin-bottom: 2rem;
        }
        
        .btn-learn-more {
            padding: 0.8rem 0;
            width: 100%;
            justify-content: center;
        }
        
        .features-container {
            padding: 2rem 0.5rem;
        }
        
        .container {
            padding-left: 1rem;
            padding-right: 1rem;
            max-width: 100%;
            overflow-x: hidden;
        }
        
        .binary-stream {
            display: none;
        }
    }
    
    @media (max-width: 380px) {
        .welcome-text-base,
        .welcome-text-glitch {
            font-size: 2.2rem; /* Increased from 1.8rem for very small devices */
        }
        
        .welcome-svg {
            height: auto;
            min-height: 60px; /* Increased from 50px */
        }
        
        .hero-subtitle {
            font-size: 1rem !important;
        }
        
        .cta-button {
            padding: 0.8rem 1.5rem;
            font-size: 1rem;
            min-width: 180px;
        }
        
        .section-title {
            font-size: 1.5rem;
        }
        
        .feature-box:hover {
            transform: none;
        }
        
        .cta-buttons {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 1rem;
        }
    }
    
    /* Fix for zoom levels below 100% */
    @media screen and (min-width: 768px) {
        /* Create a consistent scaling environment */
        html {
            /* Set a base font size for better rem scaling */
            font-size: 16px;
        }
        
        /* Ensure main content section scales properly */
        .section {
            width: 100%;
            max-width: none;
        }
        
        /* Make heading scale properly with page */
        .section-title {
            font-size: min(2.5rem, 5vw);
        }
    }
    
    /* Fix for scrolling issue */
    @media (any-pointer: fine) {
        /* Optimize scrolling performance for desktop */
        html {
            scroll-behavior: auto !important; /* Disable smooth scrolling animation */
        }
        
        /* Reduce scroll-triggered animations */
        .fade-in.visible {
            transition: opacity 0.6s ease, transform 0.6s ease !important;
        }
        
        /* Disable expensive hover effects if they cause problems */
        @media screen and (max-width: 768px) {
            .feature-box:hover {
                transform: none !important;
                box-shadow: none !important;
            }
            
            .feature-box:hover .feature-icon-container {
                transform: none !important;
            }
        }
    }
    
    /* Fix for touch devices */
    @media (pointer: coarse) {
        .cta-button, 
        .btn-learn-more,
        .feature-box,
        .feature-icon-container {
            cursor: pointer;
            -webkit-tap-highlight-color: rgba(0, 255, 65, 0.2);
        }
        
        .feature-box:hover {
            transform: none;
        }
        
        .feature-box:active {
            transform: translateY(-5px);
            transition: transform 0.2s ease;
        }
        
        .cta-button:active,
        .btn-learn-more:active {
            transform: scale(0.98);
            transition: transform 0.1s ease;
        }
    }
</style>