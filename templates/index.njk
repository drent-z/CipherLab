---
layout: layout.njk
title: CipherLab - Cryptography Educational Platform
---
<section class="hero" id="home">
    <div class="row hero-row">
        <div class="col-12 text-center">
            <div class="typing-container mb-4">
                <h1 class="mega-title">Welcome to CipherLab</h1>
            </div>
            <div class="hero-content">
                <p class="mb-4 fade-in">Unlock the secrets of cryptography through interactive learning and challenges.</p>
                <div class="cta-buttons fade-in">
                    <a href="/learn/" class="cta-button"><i class="fas fa-rocket"></i> Get Started</a>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Animated binary code decoration -->
    <div class="binary-stream">01001100 01000101 01000001 01010010 01001110</div>
</section>

<section class="container section">
    <!-- Wrap heading in its own row and col for proper centering -->
    <div class="row">
        <div class="col-12 text-center">
            <h2 class="section-title mb-4 fade-in centered-heading visible">Explore Cryptography</h2>
        </div>
    </div>
    
    <div class="features-container">
        <div class="features-grid">
            <div class="feature-box fade-in visible">
                <div class="feature-icon-container">
                    <i class="fas fa-key feature-icon"></i>
                </div>
                <h2>Classic Ciphers</h2>
                <p>Learn about fundamental encryption techniques such as the Caesar cipher, Vigenère cipher, and substitution ciphers that laid the groundwork for modern cryptography.</p>
                <div class="feature-button">
                    <a href="/learn/#module1" class="btn-learn-more">Learn More <i class="fas fa-arrow-right"></i></a>
                </div>
            </div>
            
            <div class="feature-box fade-in visible">
                <div class="feature-icon-container">
                    <i class="fas fa-shield-alt feature-icon"></i>
                </div>
                <h2>Modern Encryption</h2>
                <p>Explore contemporary encryption algorithms like AES, RSA, and SHA that power today's secure communications, digital signatures, and blockchain technology.</p>
                <div class="feature-button">
                    <a href="/learn/#module2" class="btn-learn-more">Discover <i class="fas fa-arrow-right"></i></a>
                </div>
            </div>
            
            <div class="feature-box fade-in visible">
                <div class="feature-icon-container">
                    <i class="fas fa-unlock feature-icon"></i>
                </div>
                <h2>Cryptanalysis</h2>
                <p>Try hands-on challenges to analyze and break different ciphers, enhancing your problem-solving skills and understanding of security vulnerabilities.</p>
                <div class="feature-button">
                    <a href="/learn/#module5" class="btn-learn-more">Challenge Yourself <i class="fas fa-arrow-right"></i></a>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Additional CSS for this page -->
<style>
    /* Force center align all section titles */
    .section-title.centered-heading {
        text-align: center !important;
        width: 100% !important;
        max-width: 100% !important;
        margin-left: auto !important;
        margin-right: auto !important;
        display: block !important;
    }
    
    /* Force the text-center class to work properly */
    .text-center {
        text-align: center !important;
    }
    
    /* Enhanced styling for hero section with stronger spacing controls */
    .hero {
        min-height: 85vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
        padding: 3rem 1rem;
        position: relative;
        overflow: hidden;
        box-sizing: border-box;
    }
    
    /* Force row to be full-width and block-displayed */
    .hero-row {
        width: 100%;
        display: block !important;
        margin: 0 !important;
    }
    
    /* Force column to be block displayed with adequate margins */
    .hero .col-12 {
        display: block !important;
        float: none !important;
        clear: both !important;
        margin-left: auto !important;
        margin-right: auto !important;
    }
    
    /* Fix for the typing container with more aggressive styling */
    .typing-container {
        width: 100% !important;
        position: relative !important;
        margin-bottom: 5rem !important; /* Increased spacing after title */
        text-align: center !important;
        display: block !important; /* Override any flexbox layout */
        float: none !important;
        clear: both !important;
        height: auto !important;
        min-height: 0 !important;
        padding-bottom: 0 !important;
    }
    
    /* Mega title with custom styling */
    .mega-title {
        font-size: 5.5rem;
        font-weight: 800;
        text-align: center;
        color: var(--primary);
        margin: 0 auto;
        text-shadow: 0 0 5px var(--primary);
        position: relative;
        display: inline-block;
        width: 100%;
        letter-spacing: -2px;
        z-index: 1;
    }
    
    .mega-title::before,
    .mega-title::after {
        content: "Welcome to CipherLab";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        color: var(--primary);
        text-shadow: 0 0 5px var(--primary);
    }
    
    .mega-title::before {
        left: -2px;
        animation: subtle-shift 2s infinite linear;
        opacity: 0.7;
    }
    
    .mega-title::after {
        left: 2px;
        animation: subtle-shift 3s infinite linear reverse;
        opacity: 0.7;
    }
    
    @keyframes subtle-shift {
        0%, 100% {
            transform: translate(0, 0);
        }
        25% {
            transform: translate(-1px, 0);
        }
        75% {
            transform: translate(1px, 0);
        }
    }
    
    .mega-title::before {
        /* Creates left part of the glitch effect */
        left: 2px;
        text-shadow: -2px 0 var(--primary);
        clip-path: polygon(0 0, 100% 0, 100% 45%, 0 45%);
        animation: glitch-anim 2s infinite linear alternate-reverse;
    }
    
    .mega-title::after {
        /* Creates right part of the glitch effect */
        left: -2px;
        text-shadow: 2px 0 var(--primary);
        clip-path: polygon(0 55%, 100% 55%, 100% 100%, 0 100%);
        animation: glitch-anim2 3s infinite linear alternate-reverse;
    }
    
    @keyframes glitch-anim {
        0% {
            transform: translate(-1px, -1px);
        }
        25% {
            transform: translate(1px, 0);
        }
        50% {
            transform: translate(-1px, 1px);
        }
        75% {
            transform: translate(1px, -1px);
        }
        100% {
            transform: translate(-1px, -1px);
        }
    }
    
    @keyframes glitch-anim2 {
        0% {
            transform: translate(1px, 1px);
        }
        25% {
            transform: translate(-1px, 0);
        }
        50% {
            transform: translate(1px, -1px);
        }
        75% {
            transform: translate(-1px, 1px);
        }
        100% {
            transform: translate(1px, 1px);
        }
    }
    
    /* Wrap content in a new container to ensure visual separation */
    .hero-content {
        margin-top: 4rem !important; /* Increased to create more space */
        position: relative !important;
        display: block !important;
        width: 100% !important;
    }
    
    /* Add extra spacing for the paragraph that follows the title */
    .hero p.mb-4 {
        margin-top: 2rem !important;
        margin-bottom: 2rem !important;
        max-width: 800px !important;
        margin-left: auto !important;
        margin-right: auto !important;
        font-size: 1.5rem !important;
        clear: both !important;
        position: relative !important;
        z-index: 2 !important;
        display: block !important;
    }
    
    /* Make sure the CTA buttons have proper spacing */
    .cta-buttons {
        margin-top: 1rem !important;
        clear: both !important;
        position: relative !important;
        z-index: 2 !important;
        display: block !important;
    }
    
    /* Improved features container */
    .features-container {
        padding: 3rem 0;
        width: 100%;
        max-width: 1200px;
        margin: 0 auto;
    }
    
    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
        width: 100%;
    }
    
    /* Enhanced feature box styling */
    .feature-box {
        background-color: var(--medium-dark);
        border: 1px solid var(--primary);
        border-radius: 12px;
        padding: 2.5rem;
        position: relative;
        transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        overflow: hidden;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        height: 100%;
    }
    
    .feature-box::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(to bottom, rgba(0, 255, 65, 0.05) 0%, transparent 70%);
        z-index: 0;
    }
    
    .feature-box:hover {
        transform: translateY(-15px);
        box-shadow: 0 15px 30px rgba(0, 0, 0, 0.4), 0 0 20px var(--primary);
        border-color: var(--secondary);
    }
    
    /* Improved icon container */
    .feature-icon-container {
        width: 80px;
        height: 80px;
        border-radius: 50%;
        background: linear-gradient(135deg, var(--primary-dark), var(--primary));
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 1.5rem;
        position: relative;
        box-shadow: 0 0 15px var(--primary-dark);
        z-index: 1;
        transition: all 0.3s ease;
    }
    
    .feature-icon {
        font-size: 2.5rem;
        color: var(--dark);
        transition: all 0.3s ease;
    }
    
    .feature-box:hover .feature-icon-container {
        transform: scale(1.1) rotate(5deg);
        background: linear-gradient(135deg, var(--secondary), var(--primary));
        box-shadow: 0 0 20px var(--secondary);
    }
    
    /* Title styling */
    .feature-box h2 {
        color: var(--primary);
        font-size: 1.8rem;
        margin-bottom: 1.2rem;
        transition: all 0.3s ease;
        position: relative;
        z-index: 1;
    }
    
    .feature-box h2::after {
        content: "";
        position: absolute;
        bottom: -8px;
        left: 50%;
        transform: translateX(-50%);
        width: 40px;
        height: 3px;
        background-color: var(--primary);
        transition: all 0.3s ease;
    }
    
    .feature-box:hover h2 {
        color: var(--secondary);
        text-shadow: 0 0 5px var(--secondary);
    }
    
    .feature-box:hover h2::after {
        width: 60px;
        background-color: var(--secondary);
    }
    
    /* Learn more button styling */
    .btn-learn-more {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        color: var(--primary);
        text-decoration: none;
        font-weight: 600;
        padding: 0.5rem 0;
        transition: all 0.3s ease;
        position: relative;
        z-index: 1;
    }
    
    /* Add margin to push the content down from the buttons */
    .feature-button {
        margin-top: auto;
        padding-top: 1.5rem;
    }
    
    /* Make sure paragraphs have consistent height */
    .feature-box p {
        color: var(--text);
        font-size: 1rem;
        line-height: 1.7;
        margin-bottom: 1.5rem;
        z-index: 1;
        position: relative;
        flex-grow: 1;
    }
    
    .btn-learn-more::after {
        content: "";
        position: absolute;
        bottom: 0;
        left: 0;
        width: 0;
        height: 2px;
        background-color: var(--secondary);
        transition: width 0.3s ease;
    }
    
    .btn-learn-more:hover {
        color: var(--secondary);
        transform: translateX(3px);
    }
    
    .btn-learn-more:hover::after {
        width: 100%;
    }
    
    .btn-learn-more i {
        transition: transform 0.3s ease;
    }
    
    .btn-learn-more:hover i {
        transform: translateX(3px);
    }
    
    .cta-button-alt {
        background: transparent;
        border: 2px solid var(--primary);
        color: var(--primary);
    }
    
    .cta-button-alt:hover {
        background-color: var(--primary);
        color: var(--dark);
    }
    
    .binary-stream {
        position: absolute;
        bottom: 20px;
        font-family: 'IBM Plex Mono', monospace;
        color: var(--primary);
        opacity: 0.5;
        font-size: 1rem;
        white-space: nowrap;
        animation: moveLeft 15s linear infinite;
    }
    
    @keyframes moveLeft {
        0% { transform: translateX(100vw); }
        100% { transform: translateX(-100%); }
    }
    
    /* Responsive adjustments */
    @media (max-width: 992px) {
        .features-grid {
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 1.5rem;
        }
    }
    
    @media (max-width: 768px) {
        .feature-box {
            padding: 2rem;
        }
        
        .hero-welcome-text {
            font-size: 3.5rem !important;
            min-height: 4rem !important;
        }
    }
    
    @media (max-width: 576px) {
        .features-grid {
            grid-template-columns: 1fr;
        }
        
        .feature-box {
            padding: 1.5rem;
            max-width: 100%;
        }
        
        .hero-welcome-text {
            font-size: 3rem !important;
            min-height: 3.5rem !important;
        }
        
        .feature-icon-container {
            width: 70px;
            height: 70px;
        }
        
        .feature-icon {
            font-size: 2rem;
        }
    }
</style>