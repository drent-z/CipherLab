---
layout: layout.njk
title: CipherLab - Cryptography Educational Platform
---
<section class="hero" id="home">
    <div class="row hero-row">
        <div class="col-12 text-center">
            <div class="typing-container mb-4">
                <!-- EXACT SAME STRUCTURE AS NAVBAR, JUST LARGER -->
                <div class="welcome-container">
                    <svg width="100%" height="120" viewBox="0 0 700 120" class="welcome-svg" aria-hidden="true">
                        <text x="50%" y="80" class="welcome-text-base" text-anchor="middle">Welcome to CipherLab</text>
                        <text x="50%" y="80" class="welcome-text-glitch welcome-text-glitch1" text-anchor="middle">Welcome to CipherLab</text>
                        <text x="50%" y="80" class="welcome-text-glitch welcome-text-glitch2" text-anchor="middle">Welcome to CipherLab</text>
                    </svg>
                </div>
            </div>
            <div class="hero-content">
                <p class="mb-4 fade-in">Unlock the secrets of cryptography through interactive learning and challenges.</p>
                <div class="cta-buttons fade-in">
                    <a href="/learn/" class="cta-button"><i class="fas fa-rocket"></i> Get Started</a>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Animated binary code decoration -->
    <div class="binary-stream">01001100 01000101 01000001 01010010 01001110</div>
</section>

<section class="container section">
    <!-- Wrap heading in its own row and col for proper centering -->
    <div class="row">
        <div class="col-12 text-center">
            <h2 class="section-title mb-4 fade-in centered-heading visible">Explore Cryptography</h2>
        </div>
    </div>
    
    <div class="features-container">
        <div class="features-grid">
            <div class="feature-box fade-in visible">
                <div class="feature-icon-container">
                    <i class="fas fa-key feature-icon"></i>
                </div>
                <h2>Classic Ciphers</h2>
                <p>Learn about fundamental encryption techniques such as the Caesar cipher, Vigenère cipher, and substitution ciphers that laid the groundwork for modern cryptography.</p>
                <div class="feature-button">
                    <a href="/learn/#module1" class="btn-learn-more">Learn More <i class="fas fa-arrow-right"></i></a>
                </div>
            </div>
            
            <div class="feature-box fade-in visible">
                <div class="feature-icon-container">
                    <i class="fas fa-shield-alt feature-icon"></i>
                </div>
                <h2>Modern Encryption</h2>
                <p>Explore contemporary encryption algorithms like AES, RSA, and SHA that power today's secure communications, digital signatures, and blockchain technology.</p>
                <div class="feature-button">
                    <a href="/learn/#module2" class="btn-learn-more">Discover <i class="fas fa-arrow-right"></i></a>
                </div>
            </div>
            
            <div class="feature-box fade-in visible">
                <div class="feature-icon-container">
                    <i class="fas fa-unlock feature-icon"></i>
                </div>
                <h2>Cryptanalysis</h2>
                <p>Try hands-on challenges to analyze and break different ciphers, enhancing your problem-solving skills and understanding of security vulnerabilities.</p>
                <div class="feature-button">
                    <a href="/learn/#module5" class="btn-learn-more">Challenge Yourself <i class="fas fa-arrow-right"></i></a>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Additional CSS for this page -->
<style>
    /* Fix horizontal overflow */
    html, body {
        max-width: 100%;
        overflow-x: hidden;
    }
    
    /* Force center align all section titles */
    .section-title.centered-heading {
        text-align: center !important;
        width: 100% !important;
        max-width: 100% !important;
        margin-left: auto !important;
        margin-right: auto !important;
        display: block !important;
    }
    
    /* Force the text-center class to work properly */
    .text-center {
        text-align: center !important;
    }
    
    /* Enhanced styling for hero section with stronger spacing controls */
    .hero {
        min-height: 85vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
        padding: 3rem 1rem;
        position: relative;
        overflow: hidden;
        box-sizing: border-box;
        width: 100%;
        max-width: 100vw;
    }
    
    /* Force row to be full-width and block-displayed */
    .hero-row {
        width: 100%;
        display: block !important;
        margin: 0 !important;
        max-width: 100vw;
        overflow: hidden;
    }
    
    /* Force column to be block displayed with adequate margins */
    .hero .col-12 {
        display: block !important;
        float: none !important;
        clear: both !important;
        margin-left: auto !important;
        margin-right: auto !important;
        max-width: 100vw;
        overflow: hidden;
    }
    
    /* Fix for the typing container with more aggressive styling */
    .typing-container {
        width: 100% !important;
        position: relative !important;
        margin-bottom: 5rem !important; /* Increased spacing after title */
        text-align: center !important;
        display: block !important; /* Override any flexbox layout */
        float: none !important;
        clear: both !important;
        height: auto !important;
        min-height: 0 !important;
        padding-bottom: 0 !important;
        max-width: 100vw;
        overflow: hidden;
    }
    
    /* WELCOME TEXT STYLES - EXACT COPY OF NAVBAR STYLES BUT LARGER */
    .welcome-container {
        display: block;
        text-align: center;
        margin: 0 auto;
        height: auto;
        position: relative;
        width: 100%;
        max-width: 100vw;
        overflow: hidden;
    }
    
    /* SVG container for welcome text */
    .welcome-svg {
        max-width: 100%;
        height: auto;
        overflow: visible;
    }
    
    .welcome-text-base {
        font-family: 'IBM Plex Mono', monospace;
        font-weight: 700;
        font-size: 80px;
        fill: var(--primary);
        filter: drop-shadow(0 0 2px var(--primary)) drop-shadow(0 0 5px var(--primary));
        transition: all 0.3s ease;
        animation: pulse-glow 3s infinite alternate;
        letter-spacing: -0.5px;
    }
    
    .welcome-text-glitch {
        font-family: 'IBM Plex Mono', monospace;
        font-weight: 700;
        font-size: 80px;
        opacity: 0.65;
        position: absolute;
        top: 0;
        left: 0;
        letter-spacing: -0.5px;
    }
    
    .welcome-text-glitch1 {
        fill: var(--primary);
        filter: drop-shadow(0 0 2px var(--primary));
        animation: hard-glitch1 2s infinite steps(1);
        clip-path: polygon(0 0, 100% 0, 100% 45%, 0 45%);
    }
    
    .welcome-text-glitch2 {
        fill: var(--primary); 
        filter: drop-shadow(0 0 2px var(--primary));
        animation: hard-glitch2 1.5s infinite steps(1);
        clip-path: polygon(0 60%, 100% 60%, 100% 100%, 0 100%);
    }
    
    /* Force center align all section titles */
    .section-title.centered-heading {
        text-align: center !important;
    }
    
    /* Wrap content in a new container to ensure visual separation */
    .hero-content {
        margin-top: 4rem !important; /* Increased to create more space */
        position: relative !important;
        display: block !important;
        width: 100% !important;
    }
    
    /* Add extra spacing for the paragraph that follows the title */
    .hero p.mb-4 {
        margin-top: 2rem !important;
        margin-bottom: 2rem !important;
        max-width: 800px !important;
        margin-left: auto !important;
        margin-right: auto !important;
        font-size: 1.5rem !important;
        clear: both !important;
        position: relative !important;
        z-index: 2 !important;
        display: block !important;
    }
    
    /* Make sure the CTA buttons have proper spacing */
    .cta-buttons {
        margin-top: 1rem !important;
        clear: both !important;
        position: relative !important;
        z-index: 2 !important;
        display: block !important;
    }
    
    /* Make the button more touch-friendly for mobile */
    .cta-button {
        display: inline-block;
        padding: 1rem 2.5rem;
        font-size: 1.2rem;
        border-radius: 4px;
        transition: all 0.3s ease;
        background: linear-gradient(45deg, var(--primary-dark), var(--primary));
        color: var(--dark);
        border: 1px solid var(--primary);
        text-decoration: none;
        font-weight: 700;
    }
    
    /* Improved features container */
    .features-container {
        padding: 3rem 0;
        width: 100%;
        max-width: 1200px;
        margin: 0 auto;
        overflow: hidden;
    }
    
    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
        width: 100%;
    }
    
    /* Enhanced feature box styling with fixed border issue */
    .feature-box {
        background-color: var(--medium-dark);
        border: 1px solid var(--primary);
        border-radius: 12px;
        padding: 2.5rem;
        position: relative;
        transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        overflow: hidden;
        box-shadow: none !important; /* Remove any shadow */
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        height: 100%;
        /* Fix for the green border on the right */
        border-right: 1px solid var(--primary) !important;
        border-right-width: 1px !important;
        border-right-style: solid !important;
        border-right-color: var(--primary) !important;
        max-width: 100%;
    }
    
    /* Remove all pseudo-elements that might be causing the green border */
    .feature-box::before, .feature-box::after {
        content: none !important;
        display: none !important;
    }
    
    .feature-box::before {
        display: none !important;
    }
    
    .feature-box:hover {
        transform: translateY(-15px);
        box-shadow: 0 15px 30px rgba(0, 0, 0, 0.4) !important;
        border-color: var(--secondary);
        border-right-color: var(--secondary) !important;
    }
    
    /* Improved icon container with better alignment */
    .feature-icon-container {
        width: 80px;
        height: 80px;
        border-radius: 50%;
        background: linear-gradient(135deg, var(--primary-dark), var(--primary));
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 1.5rem;
        position: relative;
        box-shadow: 0 0 15px var(--primary-dark);
        z-index: 1;
        transition: all 0.3s ease;
    }
    
    .feature-icon {
        font-size: 2.5rem;
        color: var(--dark);
        transition: all 0.3s ease;
        line-height: 1;
        margin: 0; /* Remove any default margins */
        padding: 0; /* Remove any default padding */
        display: flex; /* Ensure the icon is properly centered */
        align-items: center;
        justify-content: center;
        height: 100%;
    }
    
    .feature-box:hover .feature-icon-container {
        transform: scale(1.1) rotate(5deg);
        background: linear-gradient(135deg, var(--secondary), var(--primary));
        box-shadow: 0 0 20px var(--secondary);
    }
    
    /* Title styling */
    .feature-box h2 {
        color: var(--primary);
        font-size: 1.8rem;
        margin-bottom: 1.2rem;
        transition: all 0.3s ease;
        position: relative;
        z-index: 1;
    }
    
    .feature-box h2::after {
        content: "";
        position: absolute;
        bottom: -8px;
        left: 50%;
        transform: translateX(-50%);
        width: 40px;
        height: 3px;
        background-color: var(--primary);
        transition: all 0.3s ease;
    }
    
    .feature-box:hover h2 {
        color: var(--secondary);
        text-shadow: 0 0 5px var(--secondary);
    }
    
    .feature-box:hover h2::after {
        width: 60px;
        background-color: var(--secondary);
    }
    
    /* Learn more button styling */
    .btn-learn-more {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        color: var(--primary);
        text-decoration: none;
        font-weight: 600;
        padding: 0.5rem 0;
        transition: all 0.3s ease;
        position: relative;
        z-index: 1;
    }
    
    /* Add margin to push the content down from the buttons */
    .feature-button {
        margin-top: auto;
        padding-top: 1.5rem;
    }
    
    /* Make sure paragraphs have consistent height */
    .feature-box p {
        color: var(--text);
        font-size: 1rem;
        line-height: 1.7;
        margin-bottom: 1.5rem;
        z-index: 1;
        position: relative;
        flex-grow: 1;
    }
    
    .btn-learn-more::after {
        content: "";
        position: absolute;
        bottom: 0;
        left: 0;
        width: 0;
        height: 2px;
        background-color: var(--secondary);
        transition: width 0.3s ease;
    }
    
    .btn-learn-more:hover {
        color: var(--secondary);
        transform: translateX(3px);
    }
    
    .btn-learn-more:hover::after {
        width: 100%;
    }
    
    .btn-learn-more i {
        transition: transform 0.3s ease;
    }
    
    .btn-learn-more:hover i {
        transform: translateX(3px);
    }
    
    .cta-button-alt {
        background: transparent;
        border: 2px solid var(--primary);
        color: var(--primary);
    }
    
    .cta-button-alt:hover {
        background-color: var(--primary);
        color: var(--dark);
    }
    
    .binary-stream {
        position: absolute;
        bottom: 20px;
        font-family: 'IBM Plex Mono', monospace;
        color: var(--primary);
        opacity: 0.5;
        font-size: 1rem;
        white-space: nowrap;
        animation: moveLeft 15s linear infinite;
    }
    
    @keyframes moveLeft {
        0% { transform: translateX(100vw); }
        100% { transform: translateX(-100%); }
    }
    
    /* Enhanced section title styling */
    .section-title {
        font-size: 2.5rem;
        font-weight: 700;
        color: var(--primary);
        position: relative;
        display: inline-block;
        margin-bottom: 3rem;
        text-shadow: 0 0 5px var(--primary);
    }
    
    .section-title::after {
        content: "";
        position: absolute;
        bottom: -15px;
        left: 50%;
        transform: translateX(-50%);
        width: 80px;
        height: 3px;
        background: linear-gradient(to right, var(--primary), var(--secondary));
        transition: width 0.3s ease;
    }
    
    .section-title:hover::after {
        width: 120px;
    }
    
    /* ENHANCED MOBILE RESPONSIVENESS */
    
    /* Large devices (laptops/desktops) */
    @media (max-width: 992px) {
        .welcome-text-base,
        .welcome-text-glitch {
            font-size: 70px;
        }
        
        .features-grid {
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 1.5rem;
        }
        
        .section-title {
            font-size: 2.2rem;
        }
        
        .hero {
            min-height: 80vh;
        }
    }
    
    /* Medium devices (tablets) */
    @media (max-width: 768px) {
        .welcome-text-base,
        .welcome-text-glitch {
            font-size: 60px; /* Larger on tablets */
        }
        
        /* Adjust SVG height for better proportions */
        .welcome-svg {
            height: auto;
            min-height: 100px;
        }
        
        .feature-box {
            padding: 2rem;
            margin-bottom: 1.5rem;
            min-height: auto;
        }
        
        .hero {
            min-height: 70vh;
            padding: 2rem 1rem;
        }
        
        .hero p.mb-4 {
            font-size: 1.3rem !important;
            padding: 0 1rem;
        }
        
        .typing-container {
            margin-bottom: 3rem !important;
        }
        
        .feature-icon-container {
            width: 70px;
            height: 70px;
        }
        
        .feature-icon {
            font-size: 2.2rem;
        }
        
        .section-title {
            font-size: 2rem;
            margin-bottom: 2.5rem;
        }
        
        /* Improve tap targets for mobile */
        .cta-button {
            padding: 1rem 2rem;
            min-width: 200px; /* Ensure a good tap target width */
        }
        
        .btn-learn-more {
            padding: 0.7rem 0;
            margin-top: 0.5rem;
            /* Make tap target larger for touch */
            min-height: 44px;
            display: inline-flex;
            align-items: center;
        }
        
        /* Add more space between feature boxes on mobile */
        .features-grid {
            gap: 2rem;
        }
    }
    
    /* Small devices (phones) */
    @media (max-width: 576px) {
        .welcome-text-base,
        .welcome-text-glitch {
            font-size: 46px; /* MUCH larger for mobile to fix tiny text issue */
        }
        
        /* Adjust SVG viewBox for better mobile display */
        .welcome-svg {
            height: auto;
            min-height: 80px;
            overflow: visible;
        }
        
        /* Adjust SVG position if needed */
        .welcome-svg text {
            dominant-baseline: middle;
        }
        
        .features-grid {
            grid-template-columns: 1fr; /* Force single column on phones */
        }
        
        .feature-box {
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            /* Make sure boxes are fully visible on smaller screens */
            width: 100%;
        }
        
        .hero {
            min-height: 60vh;
            padding: 1.5rem 1rem;
        }
        
        .hero p.mb-4 {
            font-size: 1.1rem !important;
            padding: 0 0.5rem;
        }
        
        .typing-container {
            margin-bottom: 2rem !important;
        }
        
        .feature-icon-container {
            width: 60px;
            height: 60px;
            margin-bottom: 1rem;
        }
        
        .feature-icon {
            font-size: 1.8rem;
        }
        
        .feature-box h2 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }
        
        .feature-box p {
            font-size: 0.95rem;
            line-height: 1.6;
        }
        
        .section-title {
            font-size: 1.7rem;
            margin-bottom: 2rem;
        }
        
        /* Better touch targets */
        .btn-learn-more {
            padding: 0.8rem 0;
            width: 100%;
            justify-content: center;
        }
        
        /* Center everything better on small screens */
        .features-container {
            padding: 2rem 0.5rem;
        }
        
        /* Ensure no horizontal overflow */
        .container {
            padding-left: 1rem;
            padding-right: 1rem;
            max-width: 100%;
            overflow-x: hidden;
        }
        
        /* Hide binary stream on very small devices */
        .binary-stream {
            display: none;
        }
    }
    
    /* Extra small devices */
    @media (max-width: 380px) {
        .welcome-text-base,
        .welcome-text-glitch {
            font-size: 38px; /* Still bigger than before */
        }
        
        /* Adjust SVG height for better proportions */
        .welcome-svg {
            height: auto;
            min-height: 70px;
        }
        
        .hero p.mb-4 {
            font-size: 1rem !important;
        }
        
        .cta-button {
            padding: 0.8rem 1.5rem;
            font-size: 1rem;
            min-width: 180px;
        }
        
        .section-title {
            font-size: 1.5rem;
        }
        
        /* Remove hover effect on mobile as it can cause issues */
        .feature-box:hover {
            transform: none;
        }
        
        /* Stack buttons vertically if needed */
        .cta-buttons {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 1rem;
        }
    }
    
    /* Better touch support by targeting mobile devices specifically */
    @media (pointer: coarse) {
        /* Enhance tap targets for all touch interfaces */
        .cta-button, 
        .btn-learn-more,
        .feature-box,
        .feature-icon-container {
            cursor: pointer;
            -webkit-tap-highlight-color: rgba(0, 255, 65, 0.2);
        }
        
        /* Disable all hover effects on touch devices for better performance */
        .feature-box:hover {
            transform: none;
        }
        
        .feature-box:active {
            transform: translateY(-5px);
            transition: transform 0.2s ease;
        }
        
        /* Add touch feedback */
        .cta-button:active,
        .btn-learn-more:active {
            transform: scale(0.98);
            transition: transform 0.1s ease;
        }
    }
</style>