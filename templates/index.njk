---
layout: layout.njk
title: CipherLab - Cryptography Educational Platform
---
<section class="hero" id="home">
    <div class="row hero-row">
        <div class="col-12 text-center">
            <div class="typing-container mb-4">
                <h1 class="glitch centered-heading hero-welcome-text visible" data-text="Welcome to CipherLab">
                    <span>Welcome to CipherLab</span>
                    <span aria-hidden="true">Welcome to CipherLab</span>
                    <span aria-hidden="true">Welcome to CipherLab</span>
                </h1>
            </div>
            <div class="hero-content">
                <p class="mb-4 fade-in">Unlock the secrets of cryptography through interactive learning and challenges.</p>
                <div class="cta-buttons fade-in">
                    <a href="/learn/" class="cta-button"><i class="fas fa-rocket"></i> Get Started</a>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Animated binary code decoration -->
    <div class="binary-stream">01001100 01000101 01000001 01010010 01001110</div>
</section>

<section class="container section">
    <!-- Wrap heading in its own row and col for proper centering -->
    <div class="row">
        <div class="col-12 text-center">
            <h2 class="section-title mb-4 fade-in centered-heading visible">Explore Cryptography</h2>
        </div>
    </div>
    
    <div class="features-container">
        <div class="features">
            <div class="feature-box fade-in visible">
                <div class="feature-icon-container">
                    <i class="fas fa-key feature-icon"></i>
                </div>
                <h2>Classic Ciphers</h2>
                <p>Learn about fundamental encryption techniques such as the Caesar cipher, Vigenère cipher, and substitution ciphers that laid the groundwork for modern cryptography.</p>
                <div class="feature-button">
                    <a href="/learn/#module1" class="btn-learn-more">Learn More <i class="fas fa-arrow-right"></i></a>
                </div>
            </div>
            
            <div class="feature-box fade-in visible">
                <div class="feature-icon-container">
                    <i class="fas fa-shield-alt feature-icon"></i>
                </div>
                <h2>Modern Encryption</h2>
                <p>Explore contemporary encryption algorithms like AES, RSA, and SHA that power today's secure communications, digital signatures, and blockchain technology.</p>
                <div class="feature-button">
                    <a href="/learn/#module2" class="btn-learn-more">Discover <i class="fas fa-arrow-right"></i></a>
                </div>
            </div>
            
            <div class="feature-box fade-in visible">
                <div class="feature-icon-container">
                    <i class="fas fa-unlock feature-icon"></i>
                </div>
                <h2>Cryptanalysis</h2>
                <p>Try hands-on challenges to analyze and break different ciphers, enhancing your problem-solving skills and understanding of security vulnerabilities.</p>
                <div class="feature-button">
                    <a href="/learn/#module5" class="btn-learn-more">Challenge Yourself <i class="fas fa-arrow-right"></i></a>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Additional CSS for this page -->
<style>
    /* Force center align all section titles */
    .section-title.centered-heading {
        text-align: center !important;
        width: 100% !important;
        max-width: 100% !important;
        margin-left: auto !important;
        margin-right: auto !important;
        display: block !important;
    }
    
    /* Force the text-center class to work properly */
    .text-center {
        text-align: center !important;
    }
    
    /* Enhanced styling for hero section with stronger spacing controls */
    .hero {
        min-height: 85vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
        padding: 3rem 1rem;
        position: relative;
        overflow: hidden;
        box-sizing: border-box;
    }
    
    /* Force row to be full-width and block-displayed */
    .hero-row {
        width: 100%;
        display: block !important;
        margin: 0 !important;
    }
    
    /* Force column to be block displayed with adequate margins */
    .hero .col-12 {
        display: block !important;
        float: none !important;
        clear: both !important;
        margin-left: auto !important;
        margin-right: auto !important;
    }
    
    /* Fix for the typing container with more aggressive styling */
    .typing-container {
        width: 100% !important;
        position: relative !important;
        margin-bottom: 5rem !important; /* Increased spacing after title */
        text-align: center !important;
        display: block !important; /* Override any flexbox layout */
        float: none !important;
        clear: both !important;
        height: auto !important;
        min-height: 0 !important;
        padding-bottom: 0 !important;
    }
    
    /* Fix for hero text centering - UPDATED to match navbar */
    .hero .glitch, .typing-container h1 {
        position: relative !important;
        text-align: center !important;
        margin: 0 auto !important;
        font-size: 4.5rem !important;
        font-weight: 800 !important;
        letter-spacing: -1px !important;
        color: var(--primary) !important;
        background: none !important;
        text-shadow: 0 0 2px var(--primary), 0 0 5px var(--primary) !important;
        animation: pulse-glow 3s infinite alternate !important;
        line-height: 1.2 !important;
        display: block !important;
        width: 100% !important;
        height: auto !important;
        min-height: 5.5rem !important;
        overflow: visible !important;
    }
    
    /* New class for making the welcome text larger with proper spacing */
    .hero-welcome-text {
        font-size: 5rem !important;
        line-height: 1.2 !important;
        margin-bottom: 0 !important; /* Remove bottom margin from h1 itself */
        padding-bottom: 0.5rem !important; /* Add padding instead */
    }
    
    /* Enhanced glitch effect for title - UPDATED to match navbar */
    .glitch {
        position: relative;
        height: auto !important;
        overflow: visible !important;
        margin-bottom: 2rem !important; /* Add bottom margin to whole glitch container */
    }
    
    .glitch span {
        height: auto !important;
        display: block !important;
    }
    
    .glitch span:first-child {
        position: relative !important;
        z-index: 2 !important;
        display: block !important;
        width: 100% !important;
    }
    
    .glitch span:nth-child(2) {
        position: absolute !important;
        top: 0 !important;
        left: 0 !important;
        width: 100% !important;
        height: 100% !important;
        color: var(--primary) !important;
        opacity: 0.65 !important;
        z-index: 1 !important;
        animation: hard-glitch1 2s infinite steps(1) !important;
        clip-path: polygon(0 0, 100% 0, 100% 50%, 0 50%) !important;
    }
    
    .glitch span:nth-child(3) {
        position: absolute !important;
        top: 0 !important;
        left: 0 !important;
        width: 100% !important;
        height: 100% !important;
        color: var(--primary) !important;
        opacity: 0.65 !important;
        z-index: 1 !important;
        animation: hard-glitch2 1.5s infinite steps(1) !important;
        clip-path: polygon(0 50%, 100% 50%, 100% 100%, 0 100%) !important;
    }
    
    /* Hard-edge "digital" glitch animation - ADDED to match navbar */
    @keyframes hard-glitch1 {
        0%, 100% {
            transform: translate(0, 0) skew(0deg);
            opacity: 0.5;
        }
        10% {
            transform: translate(-2px, 1px) skew(1deg);
            opacity: 0.4;
        }
        15% {
            transform: translate(1px, -1px) skew(-1deg);
            opacity: 0.6;
        }
        23% {
            transform: translate(0, 0) skew(0deg);
            opacity: 0.5;
        }
        35% {
            transform: translate(-1px, 1px) skew(1deg);
            opacity: 0.4;
        }
        47% {
            transform: translate(1px, 0) skew(1deg);
            opacity: 0.6;
        }
        52% {
            transform: translate(0, 0) skew(0deg);
            opacity: 0.5;
        }
        69% {
            transform: translate(-1px, -1px) skew(-1deg);
            opacity: 0.4;
        }
        82% {
            transform: translate(0, 0) skew(0deg);
            opacity: 0.5;
        }
        91% {
            transform: translate(1px, 1px) skew(1deg);
            opacity: 0.6;
        }
    }
    
    /* Hard-edge "digital" glitch animation part 2 - ADDED to match navbar */
    @keyframes hard-glitch2 {
        0%, 100% {
            transform: translate(0, 0) skew(0deg);
            opacity: 0.5;
        }
        15% {
            transform: translate(1px, 1px) skew(1deg);
            opacity: 0.6;
        }
        21% {
            transform: translate(-1px, 0) skew(-0.5deg);
            opacity: 0.4;
        }
        38% {
            transform: translate(0, 0) skew(0deg);
            opacity: 0.5;
        }
        49% {
            transform: translate(-2px, 1px) skew(1deg);
            opacity: 0.6;
        }
        62% {
            transform: translate(0, 0) skew(0deg);
            opacity: 0.5;
        }
        73% {
            transform: translate(1px, -1px) skew(-1deg);
            opacity: 0.4;
        }
        89% {
            transform: translate(0, -1px) skew(0.5deg);
            opacity: 0.5;
        }
    }
    
    /* Pulsating glow effect - ADDED to match navbar */
    @keyframes pulse-glow {
        0%, 100% {
            text-shadow: 0 0 2px var(--primary), 0 0 4px var(--primary);
        }
        50% {
            text-shadow: 0 0 3px var(--primary), 0 0 7px var(--primary);
        }
    }
    
    /* Wrap content in a new container to ensure visual separation */
    .hero-content {
        margin-top: 4rem !important; /* Increased to create more space */
        position: relative !important;
        display: block !important;
        width: 100% !important;
    }
    
    /* Add extra spacing for the paragraph that follows the title */
    .hero p.mb-4 {
        margin-top: 2rem !important;
        margin-bottom: 2rem !important;
        max-width: 800px !important;
        margin-left: auto !important;
        margin-right: auto !important;
        font-size: 1.5rem !important;
        clear: both !important;
        position: relative !important;
        z-index: 2 !important;
        display: block !important;
    }
    
    /* Make sure the CTA buttons have proper spacing */
    .cta-buttons {
        margin-top: 1rem !important;
        clear: both !important;
        position: relative !important;
        z-index: 2 !important;
        display: block !important;
    }
    
    /* Improved features container */
    .features-container {
        padding: 2rem 0;
    }
    
    .features {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 2.5rem;
        padding: 2rem 0;
    }
    
    /* Enhanced feature box styling */
    .feature-box {
        width: 350px;
        background-color: var(--medium-dark);
        border: 1px solid var(--primary);
        border-radius: 12px;
        padding: 2.5rem;
        position: relative;
        transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        overflow: hidden;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
    }
    
    .feature-box::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(to bottom, rgba(0, 255, 65, 0.05) 0%, transparent 70%);
        z-index: 0;
    }
    
    .feature-box:hover {
        transform: translateY(-15px);
        box-shadow: 0 15px 30px rgba(0, 0, 0, 0.4), 0 0 20px var(--primary);
        border-color: var(--secondary);
    }
    
    /* Improved icon container */
    .feature-icon-container {
        width: 80px;
        height: 80px;
        border-radius: 50%;
        background: linear-gradient(135deg, var(--primary-dark), var(--primary));
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 1.5rem;
        position: relative;
        box-shadow: 0 0 15px var(--primary-dark);
        z-index: 1;
        transition: all 0.3s ease;
    }
    
    .feature-icon {
        font-size: 2.5rem;
        color: var(--dark);
        transition: all 0.3s ease;
    }
    
    .feature-box:hover .feature-icon-container {
        transform: scale(1.1) rotate(5deg);
        background: linear-gradient(135deg, var(--secondary), var(--primary));
        box-shadow: 0 0 20px var(--secondary);
    }
    
    /* Title styling */
    .feature-box h2 {
        color: var(--primary);
        font-size: 1.8rem;
        margin-bottom: 1.2rem;
        transition: all 0.3s ease;
        position: relative;
        z-index: 1;
    }
    
    .feature-box h2::after {
        content: "";
        position: absolute;
        bottom: -8px;
        left: 50%;
        transform: translateX(-50%);
        width: 40px;
        height: 3px;
        background-color: var(--primary);
        transition: all 0.3s ease;
    }
    
    .feature-box:hover h2 {
        color: var(--secondary);
        text-shadow: 0 0 5px var(--secondary);
    }
    
    .feature-box:hover h2::after {
        width: 60px;
        background-color: var(--secondary);
    }
    
    .feature-box p {
        color: var(--text);
        font-size: 1rem;
        line-height: 1.7;
        margin-bottom: 1.5rem;
        z-index: 1;
        position: relative;
    }
    
    /* Learn more button styling */
    .btn-learn-more {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        color: var(--primary);
        text-decoration: none;
        font-weight: 600;
        padding: 0.5rem 0;
        transition: all 0.3s ease;
        position: relative;
        z-index: 1;
    }
    
    .btn-learn-more::after {
        content: "";
        position: absolute;
        bottom: 0;
        left: 0;
        width: 0;
        height: 2px;
        background-color: var(--secondary);
        transition: width 0.3s ease;
    }
    
    .btn-learn-more:hover {
        color: var(--secondary);
        transform: translateX(3px);
    }
    
    .btn-learn-more:hover::after {
        width: 100%;
    }
    
    .btn-learn-more i {
        transition: transform 0.3s ease;
    }
    
    .btn-learn-more:hover i {
        transform: translateX(3px);
    }
    
    .cta-button-alt {
        background: transparent;
        border: 2px solid var(--primary);
        color: var(--primary);
    }
    
    .cta-button-alt:hover {
        background-color: var(--primary);
        color: var(--dark);
    }
    
    .binary-stream {
        position: absolute;
        bottom: 20px;
        font-family: 'IBM Plex Mono', monospace;
        color: var(--primary);
        opacity: 0.5;
        font-size: 1rem;
        white-space: nowrap;
        animation: moveLeft 15s linear infinite;
    }
    
    @keyframes moveLeft {
        0% { transform: translateX(100vw); }
        100% { transform: translateX(-100%); }
    }
    
    /* Responsive adjustments */
    @media (max-width: 992px) {
        .features {
            gap: 2rem;
        }
        
        .feature-box {
            width: 100%;
            max-width: 450px;
        }
        
        .hero-welcome-text {
            font-size: 4.5rem !important;
            min-height: 5rem !important;
        }
    }
    
    @media (max-width: 768px) {
        .feature-box {
            padding: 2rem;
        }
        
        .hero-welcome-text {
            font-size: 3.5rem !important;
            min-height: 4rem !important;
        }
    }
    
    @media (max-width: 576px) {
        .feature-box {
            padding: 1.5rem;
        }
        
        .hero-welcome-text {
            font-size: 3rem !important;
            min-height: 3.5rem !important;
        }
        
        .feature-icon-container {
            width: 70px;
            height: 70px;
        }
        
        .feature-icon {
            font-size: 2rem;
        }
    }
</style>