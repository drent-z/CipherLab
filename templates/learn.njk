---
layout: layout.njk
title: Learn Cryptography - CipherLab
---
<section class="hero" id="learn-hero">
    <div class="hero-gradient-container"></div>
    <div class="row hero-row">
        <div class="col-12 text-center">
            <h1 class="page-heading">Learn Cryptography</h1>
            <div class="hero-content">
                <p class="mb-4 fade-in hero-subtitle">Master the art and science of secure communications</p>
            </div>
        </div>
    </div>
</section>

<section class="container section">
    <div class="module-selector fade-in">
        <div class="module-tabs">
            <button class="module-tab active" data-module="module1">
                <i class="fas fa-book"></i>
                <span>1. Fundamentals</span>
            </button>
            <button class="module-tab" data-module="module2">
                <i class="fas fa-history"></i>
                <span>2. Classical Ciphers</span>
            </button>
            <button class="module-tab" data-module="module3">
                <i class="fas fa-lock"></i>
                <span>3. Modern Crypto</span>
            </button>
            <button class="module-tab" data-module="module4">
                <i class="fas fa-laptop-code"></i>
                <span>4. Applications</span>
            </button>
            <button class="module-tab" data-module="module5">
                <i class="fas fa-rocket"></i>
                <span>5. Advanced Topics</span>
            </button>
            <button class="module-tab quiz-tab" data-module="final-quiz">
                <i class="fas fa-trophy"></i>
                <span>Final Quiz</span>
            </button>
        </div>
    </div>
    
    <div class="module-container fade-in">
        <!-- Module 1 Content -->
        <div class="module-content active" id="module1-content">
            <div class="module-header">
                <h2><i class="fas fa-book"></i> Fundamentals of Cryptography</h2>
                <p>Learn the basic concepts and principles that form the foundation of cryptography.</p>
            </div>
            
            <div class="module-interface">
                <div class="lessons-overview">
                    <h3><i class="fas fa-list"></i> Module Lessons</h3>
                    <p>This module covers the essential concepts you need to understand cryptography:</p>
                    
                    <div class="lesson-cards">
                        <div class="lesson-card" id="lesson1-1-card" onclick="showLesson('lesson1-1')">
                            <div class="lesson-card-header">
                                <div class="lesson-icon"><i class="fas fa-key"></i></div>
                                <h4>1.1 What is Cryptography?</h4>
                            </div>
                            <div class="lesson-card-body">
                                <p>Introduction to secret codes and their importance in protecting information.</p>
                            </div>
                            <div class="lesson-status">
                                <i id="status-lesson1-1" class="fas fa-circle"></i> <span>Not started</span>
                            </div>
                        </div>
                        
                        <div class="lesson-card" id="lesson1-2-card" onclick="showLesson('lesson1-2')">
                            <div class="lesson-card-header">
                                <div class="lesson-icon"><i class="fas fa-sitemap"></i></div>
                                <h4>1.2 Types of Cryptography</h4>
                            </div>
                            <div class="lesson-card-body">
                                <p>Exploring different cryptographic approaches and their applications.</p>
                            </div>
                            <div class="lesson-status">
                                <i id="status-lesson1-2" class="fas fa-circle"></i> <span>Not started</span>
                            </div>
                        </div>
                        
                        <div class="lesson-card" id="lesson1-3-card" onclick="showLesson('lesson1-3')">
                            <div class="lesson-card-header">
                                <div class="lesson-icon"><i class="fas fa-globe"></i></div>
                                <h4>1.3 Real-World Applications</h4>
                            </div>
                            <div class="lesson-card-body">
                                <p>How cryptography is used in everyday life to secure communications.</p>
                            </div>
                            <div class="lesson-status">
                                <i id="status-lesson1-3" class="fas fa-circle"></i> <span>Not started</span>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="module-progress">
                    <h3><i class="fas fa-chart-line"></i> Your Progress</h3>
                    <div class="progress-container">
                        <div class="progress-label">
                            <span>Module 1 Completion:</span>
                            <span id="module1-percentage">0%</span>
                        </div>
                        <div class="progress">
                            <div id="module1-progress-bar" class="progress-bar" role="progressbar" style="width: 0%;" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Module 2 Content -->
        <div class="module-content" id="module2-content">
            <div class="module-header">
                <h2><i class="fas fa-history"></i> Classical Ciphers</h2>
                <p>Discover historical encryption methods that laid the groundwork for modern cryptography.</p>
            </div>
            
            <div class="module-interface">
                <div class="lessons-overview">
                    <h3><i class="fas fa-list"></i> Module Lessons</h3>
                    <p>Explore time-tested cryptographic techniques from history:</p>
                    
                    <div class="lesson-cards">
                        <div class="lesson-card" id="lesson2-1-card" onclick="showLesson('lesson2-1')">
                            <div class="lesson-card-header">
                                <div class="lesson-icon"><i class="fas fa-shield-alt"></i></div>
                                <h4>2.1 Caesar Cipher</h4>
                            </div>
                            <div class="lesson-card-body">
                                <p>The simple shift cipher used in ancient Rome by Julius Caesar.</p>
                            </div>
                            <div class="lesson-status">
                                <i id="status-lesson2-1" class="fas fa-circle"></i> <span>Not started</span>
                            </div>
                        </div>
                        
                        <div class="lesson-card" id="lesson2-2-card" onclick="showLesson('lesson2-2')">
                            <div class="lesson-card-header">
                                <div class="lesson-icon"><i class="fas fa-table"></i></div>
                                <h4>2.2 Vigenère Cipher</h4>
                            </div>
                            <div class="lesson-card-body">
                                <p>The "indecipherable" polyalphabetic cipher that improved on Caesar's method.</p>
                            </div>
                            <div class="lesson-status">
                                <i id="status-lesson2-2" class="fas fa-circle"></i> <span>Not started</span>
                            </div>
                        </div>
                        
                        <div class="lesson-card" id="lesson2-3-card" onclick="showLesson('lesson2-3')">
                            <div class="lesson-card-header">
                                <div class="lesson-icon"><i class="fas fa-random"></i></div>
                                <h4>2.3 Substitution Cipher</h4>
                            </div>
                            <div class="lesson-card-body">
                                <p>Replacing letters with other symbols or characters to hide messages.</p>
                            </div>
                            <div class="lesson-status">
                                <i id="status-lesson2-3" class="fas fa-circle"></i> <span>Not started</span>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="module-progress">
                    <h3><i class="fas fa-chart-line"></i> Your Progress</h3>
                    <div class="progress-container">
                        <div class="progress-label">
                            <span>Module 2 Completion:</span>
                            <span id="module2-percentage">0%</span>
                        </div>
                        <div class="progress">
                            <div id="module2-progress-bar" class="progress-bar" role="progressbar" style="width: 0%;" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Module 3 Content -->
        <div class="module-content" id="module3-content">
            <div class="module-header">
                <h2><i class="fas fa-lock"></i> Modern Cryptography</h2>
                <p>Explore contemporary encryption methods that secure our digital communications today.</p>
            </div>
            
            <div class="module-interface">
                <div class="lessons-overview">
                    <h3><i class="fas fa-list"></i> Module Lessons</h3>
                    <p>Learn about cutting-edge techniques that power today's security:</p>
                    
                    <div class="lesson-cards">
                        <div class="lesson-card" id="lesson3-1-card" onclick="showLesson('lesson3-1')">
                            <div class="lesson-card-header">
                                <div class="lesson-icon"><i class="fas fa-key"></i></div>
                                <h4>3.1 Symmetric Encryption</h4>
                            </div>
                            <div class="lesson-card-body">
                                <p>Using the same key for encryption and decryption of sensitive data.</p>
                            </div>
                            <div class="lesson-status">
                                <i id="status-lesson3-1" class="fas fa-circle"></i> <span>Not started</span>
                            </div>
                        </div>
                        
                        <div class="lesson-card" id="lesson3-2-card" onclick="showLesson('lesson3-2')">
                            <div class="lesson-card-header">
                                <div class="lesson-icon"><i class="fas fa-exchange-alt"></i></div>
                                <h4>3.2 Asymmetric Encryption</h4>
                            </div>
                            <div class="lesson-card-body">
                                <p>Public and private key cryptography that revolutionized secure communications.</p>
                            </div>
                            <div class="lesson-status">
                                <i id="status-lesson3-2" class="fas fa-circle"></i> <span>Not started</span>
                            </div>
                        </div>
                        
                        <div class="lesson-card" id="lesson3-3-card" onclick="showLesson('lesson3-3')">
                            <div class="lesson-card-header">
                                <div class="lesson-icon"><i class="fas fa-fingerprint"></i></div>
                                <h4>3.3 Hash Functions</h4>
                            </div>
                            <div class="lesson-card-body">
                                <p>One-way transformation of data for integrity checking and security.</p>
                            </div>
                            <div class="lesson-status">
                                <i id="status-lesson3-3" class="fas fa-circle"></i> <span>Not started</span>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="module-progress">
                    <h3><i class="fas fa-chart-line"></i> Your Progress</h3>
                    <div class="progress-container">
                        <div class="progress-label">
                            <span>Module 3 Completion:</span>
                            <span id="module3-percentage">0%</span>
                        </div>
                        <div class="progress">
                            <div id="module3-progress-bar" class="progress-bar" role="progressbar" style="width: 0%;" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Module 4 Content -->
        <div class="module-content" id="module4-content">
            <div class="module-header">
                <h2><i class="fas fa-laptop-code"></i> Applications</h2>
                <p>Learn how cryptography is applied in real-world systems and protocols.</p>
            </div>
            
            <div class="module-interface">
                <div class="lessons-overview">
                    <h3><i class="fas fa-list"></i> Module Lessons</h3>
                    <p>See how cryptography powers the security in systems we use daily:</p>
                    
                    <div class="lesson-cards">
                        <div class="lesson-card" id="lesson4-1-card" onclick="showLesson('lesson4-1')">
                            <div class="lesson-card-header">
                                <div class="lesson-icon"><i class="fas fa-lock"></i></div>
                                <h4>4.1 Secure Communications</h4>
                            </div>
                            <div class="lesson-card-body">
                                <p>TLS, SSL, and secure messaging protocols that protect your online activity.</p>
                            </div>
                            <div class="lesson-status">
                                <i id="status-lesson4-1" class="fas fa-circle"></i> <span>Not started</span>
                            </div>
                        </div>
                        
                        <div class="lesson-card" id="lesson4-2-card" onclick="showLesson('lesson4-2')">
                            <div class="lesson-card-header">
                                <div class="lesson-icon"><i class="fas fa-signature"></i></div>
                                <h4>4.2 Digital Signatures</h4>
                            </div>
                            <div class="lesson-card-body">
                                <p>Authentication and non-repudiation techniques for document verification.</p>
                            </div>
                            <div class="lesson-status">
                                <i id="status-lesson4-2" class="fas fa-circle"></i> <span>Not started</span>
                            </div>
                        </div>
                        
                        <div class="lesson-card" id="lesson4-3-card" onclick="showLesson('lesson4-3')">
                            <div class="lesson-card-header">
                                <div class="lesson-icon"><i class="fas fa-shield-alt"></i></div>
                                <h4>4.3 Security Protocols</h4>
                            </div>
                            <div class="lesson-card-body">
                                <p>How systems implement cryptography to protect data and users.</p>
                            </div>
                            <div class="lesson-status">
                                <i id="status-lesson4-3" class="fas fa-circle"></i> <span>Not started</span>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="module-progress">
                    <h3><i class="fas fa-chart-line"></i> Your Progress</h3>
                    <div class="progress-container">
                        <div class="progress-label">
                            <span>Module 4 Completion:</span>
                            <span id="module4-percentage">0%</span>
                        </div>
                        <div class="progress">
                            <div id="module4-progress-bar" class="progress-bar" role="progressbar" style="width: 0%;" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Module 5 Content -->
        <div class="module-content" id="module5-content">
            <div class="module-header">
                <h2><i class="fas fa-rocket"></i> Advanced Topics</h2>
                <p>Dive into cutting-edge cryptographic concepts shaping the future of security.</p>
            </div>
            
            <div class="module-interface">
                <div class="lessons-overview">
                    <h3><i class="fas fa-list"></i> Module Lessons</h3>
                    <p>Explore the frontier of cryptographic research and emerging technologies:</p>
                    
                    <div class="lesson-cards">
                        <div class="lesson-card" id="lesson5-1-card" onclick="showLesson('lesson5-1')">
                            <div class="lesson-card-header">
                                <div class="lesson-icon"><i class="fas fa-atom"></i></div>
                                <h4>5.1 Quantum Cryptography</h4>
                            </div>
                            <div class="lesson-card-body">
                                <p>Securing communications using quantum physics principles and techniques.</p>
                            </div>
                            <div class="lesson-status">
                                <i id="status-lesson5-1" class="fas fa-circle"></i> <span>Not started</span>
                            </div>
                        </div>
                        
                        <div class="lesson-card" id="lesson5-2-card" onclick="showLesson('lesson5-2')">
                            <div class="lesson-card-header">
                                <div class="lesson-icon"><i class="fas fa-eye-slash"></i></div>
                                <h4>5.2 Zero-Knowledge Proofs</h4>
                            </div>
                            <div class="lesson-card-body">
                                <p>Proving knowledge without revealing information - the future of privacy.</p>
                            </div>
                            <div class="lesson-status">
                                <i id="status-lesson5-2" class="fas fa-circle"></i> <span>Not started</span>
                            </div>
                        </div>
                        
                        <div class="lesson-card" id="lesson5-3-card" onclick="showLesson('lesson5-3')">
                            <div class="lesson-card-header">
                                <div class="lesson-icon"><i class="fas fa-user-secret"></i></div>
                                <h4>5.3 Future of Cryptography</h4>
                            </div>
                            <div class="lesson-card-body">
                                <p>Emerging trends and research directions in cryptographic methods.</p>
                            </div>
                            <div class="lesson-status">
                                <i id="status-lesson5-3" class="fas fa-circle"></i> <span>Not started</span>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="module-progress">
                    <h3><i class="fas fa-chart-line"></i> Your Progress</h3>
                    <div class="progress-container">
                        <div class="progress-label">
                            <span>Module 5 Completion:</span>
                            <span id="module5-percentage">0%</span>
                        </div>
                        <div class="progress">
                            <div id="module5-progress-bar" class="progress-bar" role="progressbar" style="width: 0%;" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Final Quiz Content -->
        <div class="module-content" id="final-quiz-content">
            <div class="module-header">
                <h2><i class="fas fa-trophy"></i> Final Cryptography Assessment</h2>
                <p>Test your knowledge of cryptography concepts across all five modules.</p>
            </div>
            
            <div class="module-interface">
                <div class="final-quiz-overview">
                    <div class="quiz-intro card">
                        <div class="card-body">
                            <h3 class="text-center mb-4"><i class="fas fa-graduation-cap"></i> Comprehensive Final Quiz</h3>
                            <p>This comprehensive assessment evaluates your understanding of cryptography across all modules:</p>
                            
                            <div class="quiz-modules">
                                <div class="quiz-module">
                                    <div class="quiz-module-icon"><i class="fas fa-book"></i></div>
                                    <div class="quiz-module-details">
                                        <h4>Module 1: Fundamentals</h4>
                                        <div class="module-completion-bar">
                                            <div class="completion-indicator" id="quiz-module1-completion"></div>
                                        </div>
                                    </div>
                                </div>
                                
                                <div class="quiz-module">
                                    <div class="quiz-module-icon"><i class="fas fa-history"></i></div>
                                    <div class="quiz-module-details">
                                        <h4>Module 2: Classical Ciphers</h4>
                                        <div class="module-completion-bar">
                                            <div class="completion-indicator" id="quiz-module2-completion"></div>
                                        </div>
                                    </div>
                                </div>
                                
                                <div class="quiz-module">
                                    <div class="quiz-module-icon"><i class="fas fa-lock"></i></div>
                                    <div class="quiz-module-details">
                                        <h4>Module 3: Modern Cryptography</h4>
                                        <div class="module-completion-bar">
                                            <div class="completion-indicator" id="quiz-module3-completion"></div>
                                        </div>
                                    </div>
                                </div>
                                
                                <div class="quiz-module">
                                    <div class="quiz-module-icon"><i class="fas fa-laptop-code"></i></div>
                                    <div class="quiz-module-details">
                                        <h4>Module 4: Applications</h4>
                                        <div class="module-completion-bar">
                                            <div class="completion-indicator" id="quiz-module4-completion"></div>
                                        </div>
                                    </div>
                                </div>
                                
                                <div class="quiz-module">
                                    <div class="quiz-module-icon"><i class="fas fa-rocket"></i></div>
                                    <div class="quiz-module-details">
                                        <h4>Module 5: Advanced Topics</h4>
                                        <div class="module-completion-bar">
                                            <div class="completion-indicator" id="quiz-module5-completion"></div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            
                            <div class="quiz-actions text-center mt-4">
                                <a href="/quiz/" class="quiz-button">
                                    <i class="fas fa-play-circle"></i> Start Final Quiz
                                </a>
                                <p class="quiz-note mt-3">You'll earn a certificate upon successful completion!</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Lesson Viewer Section -->
    <div class="lesson-viewer" id="lesson-viewer" style="display: none;">
        <div class="lesson-navigation">
            <button id="back-to-module" class="nav-button">
                <i class="fas fa-arrow-left"></i> Back to Module
            </button>
            <div class="lesson-progress-indicator">
                <span id="current-lesson-number">1</span> of <span id="total-module-lessons">3</span>
            </div>
            <div class="lesson-nav-buttons">
                <button id="prev-lesson" class="nav-button">
                    <i class="fas fa-chevron-left"></i> Previous
                </button>
                <button id="next-lesson" class="nav-button">
                    Next <i class="fas fa-chevron-right"></i>
                </button>
            </div>
        </div>
        
        <div class="lesson-content-wrapper">
            <div class="lesson-header">
                <h2 id="current-lesson-title">Lesson Title</h2>
                <button id="complete-lesson" class="complete-button">
                    <i class="fas fa-check-circle"></i> Mark as Complete
                </button>
            </div>
            
            <div class="lesson-content-container">
                <!-- Lesson content will be dynamically shown/hidden here -->
                {% include "learn/module1.njk" %}
                {% include "learn/module2.njk" %}
                {% include "learn/module3.njk" %}
                {% include "learn/module4.njk" %}
                {% include "learn/module5.njk" %}
            </div>
        </div>
    </div>
</section>

<section class="container section">
    <div class="row">
        <div class="col-lg-6">
            <div class="card info-card fade-in">
                <div class="card-header">
                    <h3><i class="fas fa-graduation-cap"></i> Learning Path</h3>
                </div>
                <div class="card-body">
                    <p>This course is designed to guide you through cryptography from fundamentals to advanced topics:</p>
                    <ol class="learning-path-list">
                        <li><strong>Start with Fundamentals</strong> - Understanding the core concepts</li>
                        <li><strong>Explore Classical Ciphers</strong> - Learn about historical techniques</li>
                        <li><strong>Study Modern Cryptography</strong> - Dive into current security methods</li>
                        <li><strong>See Real Applications</strong> - Learn how cryptography is used today</li>
                        <li><strong>Discover Advanced Topics</strong> - Explore cutting-edge research</li>
                    </ol>
                    <p>Each module builds on previous knowledge, but you can also jump directly to topics that interest you most.</p>
                </div>
            </div>
        </div>
        
        <div class="col-lg-6">
            <div class="card info-card fade-in">
                <div class="card-header">
                    <h3><i class="fas fa-tools"></i> Interactive Tools</h3>
                </div>
                <div class="card-body">
                    <p>Put your learning into practice with our interactive cryptography tools:</p>
                    <ul class="tools-list">
                        <li><a href="/tools/#text-encoder">Text Encoder</a> - Convert text between formats</li>
                        <li><a href="/tools/#caesar-cipher">Caesar Cipher</a> - Try the classic shift cipher</li>
                        <li><a href="/tools/#vigenere-cipher">Vigenère Cipher</a> - Experiment with polyalphabetic encryption</li>
                        <li><a href="/tools/#hash-generator">Hash Generator</a> - Create cryptographic hashes</li>
                        <li><a href="/tools/#password-generator">Password Generator</a> - Create secure passwords</li>
                    </ul>
                    <div class="text-center mt-3">
                        <a href="/tools/" class="tool-link-button">
                            <i class="fas fa-external-link-alt"></i> Explore All Tools
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <div class="overall-progress-section fade-in">
        <h3 class="text-center"><i class="fas fa-chart-line"></i> Your Overall Progress</h3>
        <div class="overall-progress-container">
            <div class="progress-details">
                <div class="progress-stat">
                    <div class="stat-value" id="completed-lessons-count">0/15</div>
                    <div class="stat-label">Lessons Completed</div>
                </div>
                <div class="progress-stat">
                    <div class="stat-value" id="completed-modules-count">0/5</div>
                    <div class="stat-label">Modules Completed</div>
                </div>
                <div class="progress-stat">
                    <div class="stat-value" id="final-quiz-status">Not Started</div>
                    <div class="stat-label">Final Quiz</div>
                </div>
            </div>
            <div class="overall-progress-bar-container">
                <div class="progress-label">
                    <span>Overall Completion:</span>
                    <span id="overall-percentage">0%</span>
                </div>
                <div class="progress">
                    <div id="overall-progress-bar" class="progress-bar" role="progressbar" style="width: 0%;" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100"></div>
                </div>
            </div>
            <div class="progress-actions">
                <button id="reset-progress" class="reset-button">
                    <i class="fas fa-redo-alt"></i> Reset Progress
                </button>
            </div>
        </div>
    </div>
</section>

<!-- Include Learn Enhancements Script -->
<script src="/assets/learn-enhancements.js"></script>

<style>
/* Learn Page Styles */
/* Hero Section */
#learn-hero {
    position: relative;
    min-height: 40vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    padding: 3rem 0;
    width: 100%;
    max-width: 100%;
    pointer-events: auto;
    z-index: 1;
}

#learn-hero .hero-row {
    width: 100%;
    max-width: 100%;
    margin: 0 !important;
    padding: 0 1rem;
    overflow: visible;
}

.page-heading {
    color: var(--primary);
    font-size: 3rem;
    margin-bottom: 1.5rem;
    text-align: center;
    font-weight: 700;
    position: relative;
    display: inline-block;
}

.hero-subtitle {
    font-size: 1.5rem !important;
    line-height: 1.6 !important;
    color: var(--text) !important;
    margin-top: 1rem !important;
    margin-bottom: 1.5rem !important;
    max-width: 800px !important;
    margin-left: auto !important;
    margin-right: auto !important;
    text-align: center !important;
}

.hero-content {
    margin-top: 1rem !important;
    position: relative !important;
    display: block !important;
    width: 100% !important;
    text-align: center !important;
}

/* Module Selector */
.module-selector {
    margin-bottom: 2rem;
}

.module-tabs {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    justify-content: center;
}

.module-tab {
    background-color: var(--medium-dark);
    border: 1px solid var(--primary-dark);
    color: var(--text);
    padding: 1rem;
    border-radius: 8px;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    min-width: 120px;
}

.module-tab i {
    font-size: 1.5rem;
    color: var(--primary);
    transition: all 0.3s ease;
}

.module-tab:hover {
    transform: translateY(-5px);
    border-color: var(--primary);
}

.module-tab.active {
    background-color: var(--primary-dark);
    border-color: var(--primary);
    color: var(--dark);
}

.module-tab.active i {
    color: var(--dark);
}

.quiz-tab {
    background-color: var(--darker);
    border: 1px solid var(--primary);
}

.quiz-tab i {
    color: var(--primary);
}

.quiz-tab.active {
    background-color: var(--primary);
}

/* Module Container */
.module-container {
    background-color: var(--medium-dark);
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 5px 20px rgba(0, 0, 0, 0.3);
    margin-bottom: 3rem;
}

.module-content {
    display: none;
}

.module-content.active {
    display: block;
}

.module-header {
    background-color: var(--darker);
    padding: 1.5rem;
    border-bottom: 1px solid var(--primary-dark);
}

.module-header h2 {
    margin-bottom: 0.5rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.module-header p {
    color: var(--text-muted);
    margin-bottom: 0;
}

.module-interface {
    padding: 2rem;
}

/* Lesson Cards */
.lessons-overview {
    margin-bottom: 2rem;
}

.lessons-overview h3 {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 1rem;
}

.lessons-overview h3 i {
    color: var(--primary);
}

.lesson-cards {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 1.5rem;
    margin-top: 1.5rem;
}

.lesson-card {
    background-color: var(--darker);
    border: 1px solid var(--primary-dark);
    border-radius: 8px;
    overflow: hidden;
    transition: all 0.3s ease;
    cursor: pointer;
}

.lesson-card:hover {
    transform: translateY(-5px);
    border-color: var(--primary);
    box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
}

.lesson-card-header {
    background-color: rgba(0, 255, 65, 0.1);
    padding: 1rem;
    display: flex;
    align-items: center;
    gap: 1rem;
    border-bottom: 1px solid var(--primary-dark);
}

.lesson-icon {
    background-color: var(--darker);
    color: var(--primary);
    width: 40px;
    height: 40px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.2rem;
    flex-shrink: 0;
}

.lesson-card-header h4 {
    margin: 0;
    color: var(--primary);
}

.lesson-card-body {
    padding: 1rem;
}

.lesson-card-body p {
    margin: 0;
}

.lesson-status {
    padding: 0.5rem 1rem;
    border-top: 1px solid var(--primary-dark);
    display: flex;
    align-items: center;
    font-size: 0.9rem;
    color: var(--text-muted);
}

.lesson-status i {
    margin-right: 0.5rem;
}

.fa-check-circle {
    color: var(--primary);
}

/* Module Progress */
.module-progress {
    padding-top: 2rem;
    border-top: 1px solid var(--primary-dark);
}

.module-progress h3 {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 1rem;
}

.module-progress h3 i {
    color: var(--primary);
}

.progress-container {
    background-color: var(--darker);
    border: 1px solid var(--primary-dark);
    border-radius: 8px;
    padding: 1.5rem;
}

.progress-label {
    display: flex;
    justify-content: space-between;
    margin-bottom: 0.5rem;
}

.progress {
    height: 10px;
    background-color: var(--dark);
    border-radius: 5px;
    overflow: hidden;
}

.progress-bar {
    background-color: var(--primary);
    border-radius: 5px;
    transition: width 0.3s ease;
}

/* Lesson Viewer */
.lesson-viewer {
    background-color: var(--medium-dark);
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 5px 20px rgba(0, 0, 0, 0.3);
    margin-bottom: 3rem;
}

.lesson-navigation {
    background-color: var(--darker);
    padding: 1rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid var(--primary-dark);
}

.lesson-progress-indicator {
    background-color: var(--medium-dark);
    padding: 0.5rem 1rem;
    border-radius: 20px;
    font-size: 0.9rem;
}

.lesson-nav-buttons {
    display: flex;
    gap: 0.5rem;
}

.nav-button {
    background-color: var(--medium-dark);
    border: 1px solid var(--primary-dark);
    color: var(--text);
    padding: 0.5rem 1rem;
    border-radius: 4px;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.nav-button:hover {
    background-color: var(--primary-dark);
    border-color: var(--primary);
    color: var(--dark);
}

.lesson-header {
    padding: 1.5rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid var(--primary-dark);
}

.lesson-header h2 {
    margin: 0;
}

.complete-button {
    background-color: var(--primary-dark);
    color: var(--dark);
    border: none;
    padding: 0.75rem 1.5rem;
    border-radius: 4px;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-weight: 600;
}

.complete-button:hover {
    background-color: var(--primary);
    transform: translateY(-3px);
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
}

.complete-button.completed {
    background-color: var(--primary);
}

.lesson-content-container {
    padding: 2rem;
}

/* Final Quiz */
.final-quiz-overview {
    padding: 1rem;
}

.quiz-intro {
    max-width: 800px;
    margin: 0 auto;
}

.quiz-modules {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin: 2rem 0;
}

.quiz-module {
    display: flex;
    align-items: center;
    gap: 1rem;
    background-color: var(--darker);
    padding: 1rem;
    border-radius: 8px;
    border: 1px solid var(--primary-dark);
}

.quiz-module-icon {
    background-color: var(--medium-dark);
    color: var(--primary);
    width: 50px;
    height: 50px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    flex-shrink: 0;
}

.quiz-module-details {
    flex-grow: 1;
}

.quiz-module-details h4 {
    margin: 0 0 0.5rem 0;
    font-size: 1.1rem;
}

.module-completion-bar {
    height: 8px;
    background-color: var(--medium-dark);
    border-radius: 4px;
    overflow: hidden;
}

.completion-indicator {
    height: 100%;
    background-color: var(--primary);
    width: 0;
    transition: width 0.3s ease;
}

.quiz-actions {
    margin-top: 2rem;
}

.quiz-button {
    background-color: var(--primary);
    color: var(--dark);
    text-decoration: none;
    padding: 1rem 2rem;
    border-radius: 8px;
    font-weight: bold;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    transition: all 0.3s ease;
}

.quiz-button:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 20px rgba(0, 255, 65, 0.3);
    color: var(--dark);
    text-decoration: none;
}

.quiz-note {
    color: var(--text-muted);
    font-size: 0.9rem;
}

/* Info Cards */
.info-card {
    background-color: var(--medium-dark);
    border: 1px solid var(--primary-dark);
    border-radius: 8px;
    overflow: hidden;
    margin-bottom: 2rem;
}

.info-card .card-header {
    background-color: var(--darker);
    padding: 1rem 1.5rem;
    border-bottom: 1px solid var(--primary-dark);
}

.info-card .card-header h3 {
    margin: 0;
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.info-card .card-header h3 i {
    color: var(--primary);
}

.info-card .card-body {
    padding: 1.5rem;
}

.learning-path-list, .tools-list {
    margin: 1rem 0;
    padding-left: 1.5rem;
}

.learning-path-list li, .tools-list li {
    margin-bottom: 0.5rem;
}

.tool-link-button {
    background-color: var(--primary-dark);
    color: var(--dark);
    text-decoration: none;
    padding: 0.5rem 1rem;
    border-radius: 4px;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    transition: all 0.3s ease;
}

.tool-link-button:hover {
    background-color: var(--primary);
    color: var(--dark);
    text-decoration: none;
}

/* Overall Progress Section */
.overall-progress-section {
    margin-top: 2rem;
    margin-bottom: 3rem;
}

.overall-progress-container {
    background-color: var(--medium-dark);
    border: 1px solid var(--primary-dark);
    border-radius: 8px;
    padding: 2rem;
    max-width: 800px;
    margin: 1.5rem auto 0;
}

.progress-details {
    display: flex;
    justify-content: space-around;
    margin-bottom: 2rem;
}

.progress-stat {
    text-align: center;
}

.stat-value {
    font-size: 2rem;
    font-weight: bold;
    color: var(--primary);
}

.stat-label {
    color: var(--text-muted);
    font-size: 0.9rem;
}

.overall-progress-bar-container {
    margin-bottom: 1.5rem;
}

.progress-actions {
    text-align: center;
}

.reset-button {
    background-color: var(--darker);
    border: 1px solid var(--primary-dark);
    color: var(--text-muted);
    padding: 0.5rem 1rem;
    border-radius: 4px;
    cursor: pointer;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
}

.reset-button:hover {
    background-color: var(--medium-dark);
    border-color: var(--primary);
    color: var(--text);
}

/* Lesson styling (keep original) */
.lesson-container {
    display: none;
}

/* Responsive Adjustments */
@media (max-width: 768px) {
    .page-heading {
        font-size: 2.5rem;
    }
    
    .module-tabs {
        grid-template-columns: repeat(2, 1fr);
        gap: 0.75rem;
    }
    
    .lesson-cards {
        grid-template-columns: 1fr;
    }
    
    .lesson-navigation, .lesson-header {
        flex-direction: column;
        gap: 1rem;
    }
    
    .progress-details {
        flex-direction: column;
        gap: 2rem;
    }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Progress tracking key
    const PROGRESS_KEY = 'cipherLabProgress';
    
    // Module and lesson data
    const moduleData = {
        module1: ['lesson1-1', 'lesson1-2', 'lesson1-3'],
        module2: ['lesson2-1', 'lesson2-2', 'lesson2-3'],
        module3: ['lesson3-1', 'lesson3-2', 'lesson3-3'],
        module4: ['lesson4-1', 'lesson4-2', 'lesson4-3'],
        module5: ['lesson5-1', 'lesson5-2', 'lesson5-3']
    };
    
    const moduleTitles = {
        module1: "Fundamentals of Cryptography",
        module2: "Classical Ciphers",
        module3: "Modern Cryptography",
        module4: "Applications",
        module5: "Advanced Topics",
        'final-quiz': "Final Assessment"
    };
    
    const lessonTitles = {
        'lesson1-1': "What is Cryptography?",
        'lesson1-2': "Types of Cryptography",
        'lesson1-3': "Real-World Applications",
        'lesson2-1': "Caesar Cipher",
        'lesson2-2': "Vigenère Cipher",
        'lesson2-3': "Substitution Cipher",
        'lesson3-1': "Symmetric Encryption",
        'lesson3-2': "Asymmetric Encryption",
        'lesson3-3': "Hash Functions",
        'lesson4-1': "Secure Communications",
        'lesson4-2': "Digital Signatures",
        'lesson4-3': "Security Protocols", 
        'lesson5-1': "Quantum Cryptography",
        'lesson5-2': "Zero-Knowledge Proofs",
        'lesson5-3': "Future of Cryptography"
    };
    
    // Current state tracking
    let currentModule = 'module1';
    let currentLesson = null;
    
    // DOM elements - Module tabs
    const moduleTabs = document.querySelectorAll('.module-tab');
    const moduleContents = document.querySelectorAll('.module-content');
    const moduleContainer = document.querySelector('.module-container');
    const lessonViewer = document.getElementById('lesson-viewer');
    
    // DOM elements - Lesson navigation
    const backToModuleBtn = document.getElementById('back-to-module');
    const prevLessonBtn = document.getElementById('prev-lesson');
    const nextLessonBtn = document.getElementById('next-lesson');
    const completeLessonBtn = document.getElementById('complete-lesson');
    const currentLessonTitle = document.getElementById('current-lesson-title');
    const currentLessonNumber = document.getElementById('current-lesson-number');
    const totalModuleLessons = document.getElementById('total-module-lessons');
    
    // DOM elements - Progress
    const resetProgressBtn = document.getElementById('reset-progress');
    const overallProgressBar = document.getElementById('overall-progress-bar');
    const overallPercentage = document.getElementById('overall-percentage');
    const completedLessonsCount = document.getElementById('completed-lessons-count');
    const completedModulesCount = document.getElementById('completed-modules-count');
    const finalQuizStatus = document.getElementById('final-quiz-status');
    
    // Progress management functions
    function loadProgress() {
        try {
            const savedProgress = localStorage.getItem(PROGRESS_KEY);
            if (savedProgress) {
                return JSON.parse(savedProgress);
            }
        } catch (error) {
            console.error('Error loading progress:', error);
        }
        return { completedLessons: [], finalQuizCompleted: false };
    }
    
    function saveProgress(progress) {
        try {
            localStorage.setItem(PROGRESS_KEY, JSON.stringify(progress));
            updateProgressDisplay();
        } catch (error) {
            console.error('Error saving progress:', error);
        }
    }
    
    function isLessonCompleted(lessonId) {
        const progress = loadProgress();
        return progress.completedLessons && progress.completedLessons.includes(lessonId);
    }
    
    function isModuleCompleted(moduleId) {
        const progress = loadProgress();
        if (!progress.completedLessons) return false;
        
        const moduleLessons = moduleData[moduleId];
        return moduleLessons.every(lessonId => progress.completedLessons.includes(lessonId));
    }
    
    function updateProgressDisplay() {
        const progress = loadProgress();
        const completedLessons = progress.completedLessons || [];
        const totalLessons = Object.values(moduleData).flat().length;
        
        // Update status indicators for all lessons
        Object.entries(moduleData).forEach(([moduleId, lessons]) => {
            lessons.forEach(lessonId => {
                const statusIcon = document.getElementById(`status-${lessonId}`);
                const statusText = statusIcon ? statusIcon.nextElementSibling : null;
                
                if (statusIcon && statusText) {
                    if (completedLessons.includes(lessonId)) {
                        statusIcon.className = 'fas fa-check-circle';
                        statusText.textContent = 'Completed';
                    } else {
                        statusIcon.className = 'fas fa-circle';
                        statusText.textContent = 'Not started';
                    }
                }
            });
            
            // Update module progress bar
            const moduleCompletedCount = lessons.filter(lessonId => 
                completedLessons.includes(lessonId)
            ).length;
            
            const modulePercentage = Math.round((moduleCompletedCount / lessons.length) * 100);
            const moduleProgressBar = document.getElementById(`${moduleId}-progress-bar`);
            const modulePercentageText = document.getElementById(`${moduleId}-percentage`);
            
            if (moduleProgressBar) {
                moduleProgressBar.style.width = `${modulePercentage}%`;
                moduleProgressBar.setAttribute('aria-valuenow', modulePercentage);
            }
            
            if (modulePercentageText) {
                modulePercentageText.textContent = `${modulePercentage}%`;
            }
            
            // Update quiz module completion indicators
            const quizModuleCompletion = document.getElementById(`quiz-${moduleId}-completion`);
            if (quizModuleCompletion) {
                quizModuleCompletion.style.width = `${modulePercentage}%`;
            }
        });
        
        // Update overall progress statistics
        const overallPercentage = Math.round((completedLessons.length / totalLessons) * 100);
        overallProgressBar.style.width = `${overallPercentage}%`;
        overallProgressBar.setAttribute('aria-valuenow', overallPercentage);
        
        document.getElementById('overall-percentage').textContent = `${overallPercentage}%`;
        document.getElementById('completed-lessons-count').textContent = `${completedLessons.length}/${totalLessons}`;
        
        const completedModules = Object.keys(moduleData).filter(moduleId => 
            isModuleCompleted(moduleId)
        ).length;
        
        document.getElementById('completed-modules-count').textContent = `${completedModules}/5`;
        
        document.getElementById('final-quiz-status').textContent = 
            progress.finalQuizCompleted ? 'Completed' : 'Not Started';
    }
    
    // Module switching
    function showModule(moduleId) {
        // Hide lesson viewer, show module container
        lessonViewer.style.display = 'none';
        moduleContainer.style.display = 'block';
        
        // Update active module tab
        moduleTabs.forEach(tab => {
            if (tab.getAttribute('data-module') === moduleId) {
                tab.classList.add('active');
            } else {
                tab.classList.remove('active');
            }
        });
        
        // Show selected module content
        moduleContents.forEach(content => {
            if (content.id === `${moduleId}-content`) {
                content.classList.add('active');
            } else {
                content.classList.remove('active');
            }
        });
        
        // Update current module
        currentModule = moduleId;
        
        // Scroll to top
        window.scrollTo({ top: 0, behavior: 'smooth' });
    }
    
    // Lesson viewing
    function showLesson(lessonId) {
        // Get module ID from lesson ID (e.g., lesson1-2 => module1)
        const moduleId = `module${lessonId.split('-')[0].replace('lesson', '')}`;
        const module = moduleData[moduleId];
        const lessonIndex = module.indexOf(lessonId);
        
        // Hide module container, show lesson viewer
        moduleContainer.style.display = 'none';
        lessonViewer.style.display = 'block';
        
        // Update lesson navigation
        currentLessonTitle.textContent = lessonTitles[lessonId];
        currentLessonNumber.textContent = lessonIndex + 1;
        totalModuleLessons.textContent = module.length;
        
        // Hide all lesson content
        document.querySelectorAll('.lesson-container').forEach(container => {
            container.style.display = 'none';
        });
        
        // Show the selected lesson
        const lessonContent = document.getElementById(`${lessonId}-content`);
        if (lessonContent) {
            lessonContent.style.display = 'block';
        }
        
        // Update complete button status
        if (isLessonCompleted(lessonId)) {
            completeLessonBtn.classList.add('completed');
            completeLessonBtn.innerHTML = '<i class="fas fa-check-circle"></i> Completed';
        } else {
            completeLessonBtn.classList.remove('completed');
            completeLessonBtn.innerHTML = '<i class="fas fa-check-circle"></i> Mark as Complete';
        }
        
        // Update navigation buttons visibility
        prevLessonBtn.style.display = lessonIndex > 0 ? 'flex' : 'none';
        nextLessonBtn.style.display = lessonIndex < module.length - 1 ? 'flex' : 'none';
        
        // Update current state
        currentModule = moduleId;
        currentLesson = lessonId;
        
        // Scroll to top
        window.scrollTo({ top: 0, behavior: 'smooth' });
    }
    
    // Event Listeners - Module tabs
    moduleTabs.forEach(tab => {
        tab.addEventListener('click', function() {
            const moduleId = this.getAttribute('data-module');
            showModule(moduleId);
        });
    });
    
    // Event Listeners - Lesson navigation
    backToModuleBtn.addEventListener('click', function() {
        showModule(currentModule);
    });
    
    prevLessonBtn.addEventListener('click', function() {
        if (!currentLesson) return;
        
        const moduleId = currentModule;
        const module = moduleData[moduleId];
        const currentIndex = module.indexOf(currentLesson);
        
        if (currentIndex > 0) {
            showLesson(module[currentIndex - 1]);
        }
    });
    
    nextLessonBtn.addEventListener('click', function() {
        if (!currentLesson) return;
        
        const moduleId = currentModule;
        const module = moduleData[moduleId];
        const currentIndex = module.indexOf(currentLesson);
        
        if (currentIndex < module.length - 1) {
            showLesson(module[currentIndex + 1]);
        }
    });
    
    completeLessonBtn.addEventListener('click', function() {
        if (!currentLesson) return;
        
        const progress = loadProgress();
        const completedLessons = progress.completedLessons || [];
        
        if (!completedLessons.includes(currentLesson)) {
            completedLessons.push(currentLesson);
            progress.completedLessons = completedLessons;
            saveProgress(progress);
            
            // Update button appearance
            completeLessonBtn.classList.add('completed');
            completeLessonBtn.innerHTML = '<i class="fas fa-check-circle"></i> Completed';
            
            // Add animation
            completeLessonBtn.classList.add('pulse-animation');
            setTimeout(() => {
                completeLessonBtn.classList.remove('pulse-animation');
            }, 1000);
        }
    });
    
    // Event Listener - Reset progress
    resetProgressBtn.addEventListener('click', function() {
        if (confirm('Are you sure you want to reset your progress? This action cannot be undone.')) {
            localStorage.removeItem(PROGRESS_KEY);
            saveProgress({ completedLessons: [], finalQuizCompleted: false });
            
            // Provide feedback
            this.textContent = 'Progress Reset!';
            setTimeout(() => {
                this.innerHTML = '<i class="fas fa-redo-alt"></i> Reset Progress';
            }, 2000);
        }
    });
    
    // Initialize lessons click handlers
    Object.entries(moduleData).forEach(([moduleId, lessons]) => {
        lessons.forEach(lessonId => {
            const lessonCard = document.getElementById(`${lessonId}-card`);
            if (lessonCard) {
                lessonCard.addEventListener('click', function() {
                    showLesson(lessonId);
                });
            }
        });
    });
    
    // Function to check answers in quick check quizzes
    window.checkAnswer = function(button, lessonId) {
        const correctAnswer = button.getAttribute('data-correct');
        const questionContainer = button.closest('.quiz-container');
        const selectedOption = questionContainer.querySelector('input[type="radio"]:checked');
        
        if (!selectedOption) {
            alert('Please select an answer first!');
            return;
        }
        
        const feedbackContainer = questionContainer.querySelector('.feedback-container');
        
        // Show feedback container
        feedbackContainer.style.display = 'block';
        
        // Check if answer is correct
        if (selectedOption.value === correctAnswer) {
            // Show correct feedback, hide incorrect
            questionContainer.querySelector('.correct-answer').style.display = 'block';
            questionContainer.querySelector('.incorrect-answer').style.display = 'none';
            
            // Highlight selected option
            selectedOption.closest('.simple-option').classList.add('correct-option');
        } else {
            // Show incorrect feedback, hide correct
            questionContainer.querySelector('.correct-answer').style.display = 'none';
            questionContainer.querySelector('.incorrect-answer').style.display = 'block';
            
            // Highlight selected option as incorrect
            selectedOption.closest('.simple-option').classList.add('incorrect-option');
            
            // Highlight correct option
            const correctOption = questionContainer.querySelector(`input[value="${correctAnswer}"]`);
            if (correctOption) {
                correctOption.closest('.simple-option').classList.add('correct-option');
            }
        }
        
        // Disable all options
        questionContainer.querySelectorAll('input[type="radio"]').forEach(input => {
            input.disabled = true;
        });
        
        // Update button
        button.textContent = 'Answered';
        button.disabled = true;
    };
    
    // Initialize progress display
    updateProgressDisplay();
    
    // Show first module on load
    showModule('module1');
});
</script>